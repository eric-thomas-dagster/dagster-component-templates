{
  "component_type": "dagster_component_templates.PreciselyDISDQCheckComponent",
  "description": "Wrap a Precisely DIS Data Quality rule set as N Dagster asset checks.\n\nExample:\n    ```yaml\n    type: dagster_community_components.PreciselyDISDQCheckComponent\n    attributes:\n      asset_key: warehouse/orders\n      dataset_id: \"ds-abc-123\"\n      rule_ids: [\"rule-001\", \"rule-002\", \"rule-003\"]\n      client_id_env_var: PRECISELY_DIS_CLIENT_ID\n      client_secret_env_var: PRECISELY_DIS_CLIENT_SECRET\n    ```\n\nThe DIS dataset is referenced by ``dataset_id`` (the DIS-side\nidentifier, not the Dagster asset key). Each rule becomes its own\nDagster asset check named ``dis_dq__{rule_id}`` on the target asset.",
  "category": "check",
  "icon": "Search",
  "attributes": {
    "asset_key": {
      "type": "string",
      "label": "Asset Key",
      "required": true,
      "description": "Dagster asset key the checks attach to (e.g. 'warehouse/orders'). Use '/' separators for nested keys."
    },
    "dataset_id": {
      "type": "string",
      "label": "Dataset ID",
      "required": true,
      "description": "DIS dataset id — the dataset the rules are bound to in DIS."
    },
    "rule_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "ui:widget": "list",
      "label": "Rule Ids",
      "required": true,
      "description": "Rule ids to execute. Pre-resolved at build_defs time so each becomes its own asset check (individually alertable + retriable). Get rule ids from the DIS UI or the GET /datasets/{dataset_id}/rules endpoint."
    },
    "client_id_env_var": {
      "type": "string",
      "label": "Client ID Env Var",
      "required": false,
      "description": "Env var with DIS OAuth2 client_id",
      "default": "PRECISELY_DIS_CLIENT_ID"
    },
    "client_secret_env_var": {
      "type": "string",
      "label": "Client Secret Env Var",
      "required": false,
      "description": "Env var with DIS OAuth2 client_secret",
      "default": "PRECISELY_DIS_CLIENT_SECRET"
    },
    "token_url": {
      "type": "string",
      "label": "Token URL",
      "required": false,
      "description": "OAuth2 token endpoint (override for non-global DIS tenants).",
      "default": null
    },
    "dq_base_url": {
      "type": "string",
      "label": "DQ Base URL",
      "required": false,
      "description": "DIS Data Quality service base URL.",
      "default": null
    },
    "poll_interval_seconds": {
      "type": "integer",
      "label": "Poll Interval Seconds",
      "required": false,
      "description": "Seconds between run-status polls while a DQ execution runs.",
      "default": 5
    },
    "timeout_seconds": {
      "type": "integer",
      "label": "Timeout Seconds",
      "required": false,
      "description": "Hard timeout (seconds) for a single DQ execution.",
      "default": 600
    },
    "severity": {
      "type": "string",
      "label": "Severity",
      "required": false,
      "description": "AssetCheckSeverity for failing checks: 'WARN' or 'ERROR'.",
      "default": "ERROR",
      "enum": [
        "WARN",
        "ERROR"
      ],
      "ui:widget": "select"
    }
  }
}
