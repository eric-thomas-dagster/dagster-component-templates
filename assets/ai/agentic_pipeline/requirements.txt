litellm>=1.30.0
requests
# Only required if you use source kind=database.
sqlalchemy>=1.4
pandas>=1.5.0
# Only required if you use the `mcp_call` op (direct MCP step in the pipeline).
# Slim install; kept as a hard dep so the op works out of the box.
mcp>=1.0.0
# Provider client SDKs (installed on demand by litellm; add explicitly if you want pin control):
#   openai
#   anthropic
#   google-generativeai
