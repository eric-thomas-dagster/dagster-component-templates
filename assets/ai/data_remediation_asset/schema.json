{
  "component_type": "dagster_community_components.DataRemediationAssetComponent",
  "description": "Apply an agent-produced remediation plan to an upstream DataFrame. Bounded action set (drop_nulls, fill_nulls, dedup, cast_type, clip_outliers, filter_range, strip_whitespace, ...) — the LLM picks by name, this component executes safely. Full audit trail in asset metadata.",
  "category": "ai",
  "icon": "Wand",
  "x-dagster-io": {
    "inputs": {"type": "dataframe", "description": "Two upstream assets: the raw DataFrame and the plan DataFrame."},
    "outputs": {"type": "dataframe", "description": "Remediated DataFrame with actions applied."}
  },
  "attributes": {
    "asset_name": {"label": "Asset Name", "type": "string", "required": true},
    "upstream_data_key": {"label": "Upstream Data Key", "type": "string", "required": true, "description": "Asset key producing the DataFrame to remediate."},
    "plan_key": {"label": "Plan Key", "type": "string", "required": true, "description": "Asset key producing the plan DataFrame (columns: column, action, params, reason)."},
    "fail_on_unknown_action": {"label": "Fail On Unknown Action", "type": "boolean", "required": false, "default": false, "description": "If True, raise on unrecognized action names."},
    "group_name": {"label": "Group Name", "type": "string", "required": false},
    "description": {"label": "Description", "type": "string", "required": false},
    "owners": {"label": "Owners", "type": "array", "required": false},
    "tags": {"label": "Tags", "type": "object", "required": false},
    "kinds": {"label": "Kinds", "type": "array", "required": false}
  }
}
