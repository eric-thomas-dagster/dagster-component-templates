{
  "component_type": "dagster_component_templates.DocumentSummarizerComponent",
  "description": "Component for summarizing documents with LLMs.",
  "category": "ai",
  "icon": "Sparkles",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Enriched DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of the asset",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame with documents to summarize",
      "required": true
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column name containing document text to summarize",
      "required": false,
      "default": "text",
      "ui:widget": "textarea"
    },
    "summary_column": {
      "type": "string",
      "label": "Summary Column",
      "description": "Column name for generated summaries",
      "required": false,
      "default": "summary"
    },
    "provider": {
      "type": "string",
      "label": "Provider",
      "description": "LLM provider",
      "required": true
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Model name",
      "required": true
    },
    "summary_type": {
      "type": "string",
      "label": "Summary Type",
      "description": "Type: ",
      "required": false,
      "default": "concise"
    },
    "max_length": {
      "type": "integer",
      "label": "Max Length",
      "description": "Max summary length in words",
      "required": false,
      "default": null
    },
    "chunk_size": {
      "type": "integer",
      "label": "Chunk Size",
      "description": "Chunk size for long documents",
      "required": false,
      "default": 3000
    },
    "use_map_reduce": {
      "type": "boolean",
      "label": "Use Map Reduce",
      "description": "Use map-reduce for long documents",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "api_key": {
      "type": "string",
      "label": "Api Key",
      "description": "API key with ${VAR_NAME} syntax",
      "required": false,
      "default": null
    },
    "temperature": {
      "type": "number",
      "label": "Temperature",
      "description": "Temperature",
      "required": false,
      "default": 0.3
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": null,
      "ui:widget": "code"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date × static (e.g. day × customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners — team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column → list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    }
  },
  "tags": [
    "ai",
    "document",
    "summarizer"
  ]
}
