{
  "component_type": "dagster_component_templates.DspyProgramComponent",
  "description": "Run a DSPy program with automatic prompt optimization against a DataFrame column using a signature-based interface.",
  "category": "ai",
  "icon": "Cpu",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame with text to process"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with DSPy program output fields appended"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column containing input text",
      "required": true
    },
    "output_column": {
      "type": "string",
      "label": "Output Column",
      "description": "Primary output column (first output field from signature)",
      "required": false,
      "default": "dspy_output"
    },
    "program_type": {
      "type": "string",
      "label": "Program Type",
      "description": "DSPy program type",
      "required": false,
      "default": "chain_of_thought",
      "ui:widget": "select",
      "enum": [
        "predict",
        "chain_of_thought",
        "react",
        "multi_chain_comparison"
      ]
    },
    "signature": {
      "type": "string",
      "label": "Signature",
      "description": "DSPy signature string e.g. \"question -> answer\" or \"document -> summary, key_points\"",
      "required": true,
      "ui:widget": "textarea"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "LLM model string",
      "required": false,
      "default": "gpt-4o-mini"
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Env var name for API key",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "dspy",
    "llm",
    "prompt-optimization",
    "chain-of-thought",
    "nlp"
  ]
}
