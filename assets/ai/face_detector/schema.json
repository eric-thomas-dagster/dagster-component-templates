{
  "component_type": "dagster_component_templates.FaceDetectorComponent",
  "description": "Detect faces in images and return bounding boxes, face count, and optional landmark positions.",
  "category": "ai",
  "icon": "SmilePlus",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "DataFrame with image file paths"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with face counts and optional bounding boxes and landmarks"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "image_column": {
      "type": "string",
      "label": "Image Column",
      "description": "Column with image file paths",
      "required": true
    },
    "count_column": {
      "type": "string",
      "label": "Count Column",
      "description": "Column to write detected face count",
      "required": false,
      "default": "face_count"
    },
    "boxes_column": {
      "type": "string",
      "label": "Boxes Column",
      "description": "If set, write list of bounding box dicts to this column",
      "required": false,
      "default": null
    },
    "landmarks_column": {
      "type": "string",
      "label": "Landmarks Column",
      "description": "If set, write facial landmarks to this column",
      "required": false,
      "default": null
    },
    "backend": {
      "type": "string",
      "label": "Backend",
      "description": "Detection backend: opencv, mediapipe, or retinaface",
      "required": false,
      "default": "opencv",
      "enum": [
        "opencv",
        "mediapipe",
        "retinaface"
      ],
      "ui:widget": "select"
    },
    "min_confidence": {
      "type": "number",
      "label": "Min Confidence",
      "description": "Minimum detection confidence (used by mediapipe backend)",
      "required": false,
      "default": 0.5
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "image",
    "vision",
    "face-detection"
  ]
}
