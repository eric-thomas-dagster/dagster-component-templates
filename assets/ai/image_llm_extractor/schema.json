{
  "component_type": "dagster_component_templates.ImageLlmExtractorComponent",
  "description": "Send images to a vision LLM (GPT-4o, Claude, Gemini) and extract structured fields as new DataFrame columns.",
  "category": "ai",
  "icon": "Scan",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "DataFrame with image file paths or URLs"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with extracted structured fields as new columns"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "image_column": {
      "type": "string",
      "label": "Image Column",
      "description": "Column with image file paths or URLs",
      "required": true
    },
    "extraction_fields": {
      "type": "object",
      "label": "Extraction Fields",
      "description": "Mapping of column_name -> description e.g. {\"product_name\": \"name of product shown\"}",
      "required": true,
      "ui:widget": "key-value"
    },
    "prompt_prefix": {
      "type": "string",
      "label": "Prompt Prefix",
      "description": "Extra instruction prepended to the extraction prompt",
      "required": false,
      "default": null,
      "ui:widget": "textarea"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Vision-capable model name in litellm format",
      "required": false,
      "default": "gpt-4o-mini"
    },
    "max_tokens": {
      "type": "integer",
      "label": "Max Tokens",
      "description": "Maximum tokens in LLM response",
      "required": false,
      "default": 500
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Environment variable holding the API key",
      "required": false,
      "default": "OPENAI_API_KEY",
      "ui:widget": "password"
    },
    "input_type": {
      "type": "string",
      "label": "Input Type",
      "description": "Input type: 'file' (base64 encode) or 'url' (pass directly)",
      "required": false,
      "default": "file",
      "enum": [
        "file",
        "url"
      ],
      "ui:widget": "select"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    }
  },
  "tags": [
    "ai",
    "image",
    "vision",
    "llm",
    "extraction"
  ],
  "connectors": {
    "left": true,
    "right": true
  }
}
