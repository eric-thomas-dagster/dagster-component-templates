{
  "component_type": "dagster_component_templates.ImageMetadataExtractorComponent",
  "description": "Extract technical metadata from images including dimensions, format, color mode, and EXIF data such as GPS coordinates and camera model.",
  "category": "ai",
  "icon": "Camera",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "DataFrame with image file paths"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with image metadata columns (dimensions, format, EXIF, GPS)"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "image_column": {
      "type": "string",
      "label": "Image Column",
      "description": "Column with image file paths",
      "required": true
    },
    "extract_exif": {
      "type": "boolean",
      "label": "Extract EXIF",
      "description": "Extract EXIF metadata (capture date, camera model)",
      "required": false,
      "default": true
    },
    "extract_gps": {
      "type": "boolean",
      "label": "Extract GPS",
      "description": "Parse GPS EXIF tags into decimal lat/lon columns",
      "required": false,
      "default": true
    },
    "output_prefix": {
      "type": "string",
      "label": "Output Prefix",
      "description": "Prefix for all output columns",
      "required": false,
      "default": "img_"
    },
    "include_histogram": {
      "type": "boolean",
      "label": "Include Histogram",
      "description": "Add color histogram as a column",
      "required": false,
      "default": false
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "image",
    "metadata",
    "exif",
    "vision"
  ]
}
