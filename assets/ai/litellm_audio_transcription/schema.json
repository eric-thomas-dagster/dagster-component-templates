{
  "component_type": "dagster_component_templates.LitellmAudioTranscriptionComponent",
  "description": "Transcribe audio files referenced in a DataFrame column using Whisper via LiteLLM and write transcription text to a new column.",
  "category": "ai",
  "icon": "Mic",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame with audio file paths"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with transcription text appended"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "audio_path_column": {
      "type": "string",
      "label": "Audio Path Column",
      "description": "Column containing local audio file paths",
      "required": true
    },
    "output_column": {
      "type": "string",
      "label": "Output Column",
      "description": "Column to write transcribed text",
      "required": false,
      "default": "transcription"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Transcription model (e.g. whisper-1)",
      "required": false,
      "default": "whisper-1"
    },
    "language": {
      "type": "string",
      "label": "Language",
      "description": "ISO 639-1 language code hint (e.g. en, es, fr)",
      "required": false,
      "default": null
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Env var name for API key",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "audio",
    "transcription",
    "whisper",
    "litellm",
    "speech-to-text"
  ]
}
