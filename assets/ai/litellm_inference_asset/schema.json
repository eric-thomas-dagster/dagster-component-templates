{
  "component_type": "dagster_component_templates.LiteLLMResource",
  "description": "Shared LiteLLM configuration for use across multiple components.",
  "category": "ai",
  "icon": "si:openai",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Enriched DataFrame"
    }
  },
  "attributes": {
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Model",
      "required": true
    },
    "api_key_env_var": {
      "type": "string",
      "label": "Api Key Env Var",
      "description": "Env var with provider API key",
      "required": false,
      "default": null
    },
    "api_base_env_var": {
      "type": "string",
      "label": "Api Base Env Var",
      "description": "Api Base Env Var",
      "required": false,
      "default": null
    },
    "temperature": {
      "type": "number",
      "label": "Temperature",
      "description": "Sampling temperature",
      "required": false,
      "default": 0.0
    },
    "max_tokens": {
      "type": "integer",
      "label": "Max Tokens",
      "description": "Max tokens per completion",
      "required": false,
      "default": 1024
    },
    "timeout_seconds": {
      "type": "integer",
      "label": "Timeout Seconds",
      "description": "Request timeout in seconds",
      "required": false,
      "default": 60
    },
    "max_retries": {
      "type": "integer",
      "label": "Max Retries",
      "description": "Max retries on failure",
      "required": false,
      "default": 3
    },
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame (e.g. ",
      "required": true
    },
    "litellm_resource_key": {
      "type": "string",
      "label": "Litellm Resource Key",
      "description": "Key of a LiteLLMResource in resources dict",
      "required": false,
      "default": null
    },
    "prompt_template": {
      "type": "string",
      "label": "Prompt Template",
      "description": "Python format string using column names: ",
      "required": true,
      "ui:widget": "textarea"
    },
    "system_prompt": {
      "type": "string",
      "label": "System Prompt",
      "description": "System prompt for chat completions",
      "required": false,
      "default": null,
      "ui:widget": "textarea"
    },
    "response_column": {
      "type": "string",
      "label": "Response Column",
      "description": "Column name to store LLM responses",
      "required": false,
      "default": "llm_response"
    },
    "batch_size": {
      "type": "integer",
      "label": "Batch Size",
      "description": "Batch Size",
      "required": false,
      "default": 10
    },
    "max_rows": {
      "type": "integer",
      "label": "Max Rows",
      "description": "Max Rows",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group name",
      "required": false,
      "default": "ai"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Description",
      "required": false,
      "default": null,
      "ui:widget": "code"
    }
  },
  "tags": [
    "ai",
    "litellm",
    "inference",
    "asset"
  ],
  "connectors": {
    "left": true,
    "right": true
  }
}
