{
  "component_type": "dagster_component_templates.LitellmStructuredOutputComponent",
  "description": "Extract structured JSON data from text using LiteLLM's JSON mode and expand extracted fields as new DataFrame columns.",
  "category": "ai",
  "icon": "Braces",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with extracted structured fields appended as new columns"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column containing input text",
      "required": true
    },
    "schema_definition": {
      "type": "object",
      "label": "Schema Definition",
      "description": "JSON Schema describing the fields to extract e.g. {\"name\": {\"type\": \"string\"}, \"age\": {\"type\": \"integer\"}}",
      "required": true,
      "ui:widget": "key-value"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "LiteLLM model string",
      "required": false,
      "default": "gpt-4o-mini"
    },
    "prompt_prefix": {
      "type": "string",
      "label": "Prompt Prefix",
      "description": "Instruction prepended to each extraction request",
      "required": false,
      "default": null,
      "ui:widget": "textarea"
    },
    "output_prefix": {
      "type": "string",
      "label": "Output Prefix",
      "description": "Prefix for extracted column names",
      "required": false,
      "default": ""
    },
    "on_error": {
      "type": "string",
      "label": "On Error",
      "description": "Error handling strategy: skip, null, or raise",
      "required": false,
      "default": "null",
      "ui:widget": "select",
      "enum": [
        "skip",
        "null",
        "raise"
      ]
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Env var name for API key",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "llm",
    "litellm",
    "structured-output",
    "extraction",
    "json"
  ]
}
