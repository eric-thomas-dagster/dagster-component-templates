type: dagster_component_templates.ModerationScorerComponent
attributes:
  asset_name: moderation_scores

  # Source asset (automatically set via lineage)
  upstream_asset_key: user_content

  # Organization
  description: "Content moderation scores and decisions"
