{
  "component_type": "dagster_component_templates.OllamaResource",
  "description": "Shared Ollama connection config for use across multiple components.",
  "category": "ai",
  "icon": "Sparkles",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Enriched DataFrame"
    }
  },
  "attributes": {
    "host": {
      "type": "string",
      "label": "Host",
      "description": "Ollama server URL",
      "required": false,
      "default": "http://localhost:11434"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Model",
      "required": true
    },
    "temperature": {
      "type": "number",
      "label": "Temperature",
      "description": "Sampling temperature",
      "required": false,
      "default": 0.0
    },
    "timeout_seconds": {
      "type": "integer",
      "label": "Timeout Seconds",
      "description": "Request timeout in seconds",
      "required": false,
      "default": 120
    },
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "ollama_host_env_var": {
      "type": "string",
      "label": "Ollama Host Env Var",
      "description": "Ollama Host Env Var",
      "required": false,
      "default": null
    },
    "ollama_resource_key": {
      "type": "string",
      "label": "Ollama Resource Key",
      "description": "Key of an OllamaResource in resources dict",
      "required": false,
      "default": null
    },
    "prompt_template": {
      "type": "string",
      "label": "Prompt Template",
      "description": "Python format string using column names: ",
      "required": true,
      "ui:widget": "textarea"
    },
    "system_prompt": {
      "type": "string",
      "label": "System Prompt",
      "description": "System prompt for the model",
      "required": false,
      "default": null,
      "ui:widget": "textarea"
    },
    "response_column": {
      "type": "string",
      "label": "Response Column",
      "description": "Column name to store model responses",
      "required": false,
      "default": "llm_response"
    },
    "batch_size": {
      "type": "integer",
      "label": "Batch Size",
      "description": "Rows per batch for progress logging",
      "required": false,
      "default": 10
    },
    "max_rows": {
      "type": "integer",
      "label": "Max Rows",
      "description": "Max Rows",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group name",
      "required": false,
      "default": "ai"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Description",
      "required": false,
      "default": null,
      "ui:widget": "code"
    }
  },
  "tags": [
    "ai",
    "ollama",
    "inference",
    "asset"
  ]
}
