{
  "component_type": "dagster_component_templates.PiiDetectorComponent",
  "description": "Detect personally identifiable information (PII) in text columns using Microsoft Presidio.",
  "category": "ai",
  "icon": "ShieldAlert",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame with a text column to scan for PII"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with PII entity lists added as a new column"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column containing text to analyze for PII",
      "required": true
    },
    "output_column": {
      "type": "string",
      "label": "Output Column",
      "description": "Column to write list of detected entity dicts",
      "required": false,
      "default": "pii_entities"
    },
    "entity_types": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Entity Types",
      "description": "Filter to these entity types e.g. PERSON, EMAIL_ADDRESS, PHONE_NUMBER. Default=all.",
      "required": false,
      "default": null,
      "ui:widget": "list"
    },
    "language": {
      "type": "string",
      "label": "Language",
      "description": "Language of the text (ISO 639-1 code)",
      "required": false,
      "default": "en"
    },
    "score_threshold": {
      "type": "number",
      "label": "Score Threshold",
      "description": "Minimum confidence score for detected entities (0-1)",
      "required": false,
      "default": 0.5
    },
    "add_count_column": {
      "type": "boolean",
      "label": "Add Count Column",
      "description": "Add a {output_column}_count column with the number of detected entities",
      "required": false,
      "default": true
    }
  },
  "tags": [
    "ai",
    "pii",
    "privacy",
    "nlp",
    "presidio"
  ]
}
