{
  "component_type": "dagster_component_templates.ShippingLabelExtractorComponent",
  "description": "Extract logistics data from shipping labels and packing slips using an LLM.",
  "category": "ai",
  "icon": "Package",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "DataFrame with document content"
    },
    "outputs": {
      "type": "dataframe",
      "description": "DataFrame with extracted fields as new columns"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "input_column": {
      "type": "string",
      "label": "Input Column",
      "description": "Column containing document text or file paths",
      "required": false,
      "default": "text"
    },
    "input_type": {
      "type": "string",
      "label": "Input Type",
      "description": "text or file",
      "required": false,
      "default": "text",
      "enum": [
        "text",
        "file"
      ],
      "ui:widget": "select"
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "LiteLLM model string (e.g. gpt-4o-mini, claude-3-haiku-20240307)",
      "required": false,
      "default": "gpt-4o-mini"
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Environment variable name holding the API key",
      "required": false,
      "default": "OPENAI_API_KEY",
      "ui:widget": "password"
    },
    "output_fields": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Output Fields",
      "description": "Fields to extract from each document",
      "required": false,
      "ui:widget": "list"
    },
    "batch_size": {
      "type": "integer",
      "label": "Batch Size",
      "description": "Rows processed per LLM call",
      "required": false,
      "default": 5
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ai",
    "extraction",
    "logistics",
    "shipping"
  ]
}
