pandas>=2.0.0
litellm>=1.30.0
fsspec>=2024.0.0
pdfplumber>=0.7.0
Pillow>=9.0.0

# Optional — install only the protocol(s) you actually list `path` or
# post_process_dir against:
#   pip install s3fs              # for s3:// URIs
#   pip install gcsfs             # for gs:// URIs
#   pip install adlfs             # for abfss:// / abfs:// / az:// URIs
