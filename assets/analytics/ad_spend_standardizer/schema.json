{
  "component_type": "dagster_component_templates.AdSpendStandardizerComponent",
  "description": "Component for standardizing advertising spend data across platforms.",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of the standardized ad spend output asset",
      "required": true
    },
    "google_ads_asset": {
      "type": "string",
      "label": "Google Ads Asset",
      "description": "Google Ads Asset",
      "required": false,
      "default": null
    },
    "facebook_ads_asset": {
      "type": "string",
      "label": "Facebook Ads Asset",
      "description": "Facebook Ads Asset",
      "required": false,
      "default": null
    },
    "other_ad_platform_asset": {
      "type": "string",
      "label": "Other Ad Platform Asset",
      "description": "Other Ad Platform Asset",
      "required": false,
      "default": null
    },
    "currency_normalization": {
      "type": "string",
      "label": "Currency Normalization",
      "description": "Currency Normalization",
      "required": false,
      "default": "USD"
    },
    "date_column_name": {
      "type": "string",
      "label": "Date Column Name",
      "description": "Name of the date column in source data",
      "required": false,
      "default": "date"
    },
    "aggregate_by_day": {
      "type": "boolean",
      "label": "Aggregate By Day",
      "description": "Aggregate By Day",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "include_zero_spend_days": {
      "type": "boolean",
      "label": "Include Zero Spend Days",
      "description": "Include days with zero spend in output",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "calculate_derived_metrics": {
      "type": "boolean",
      "label": "Calculate Derived Metrics",
      "description": "Calculate CTR, CPC, CPA, ROAS automatically",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": "",
      "ui:widget": "code"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group name",
      "required": false,
      "default": "standardizers"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    }
  },
  "tags": [
    "analytics",
    "spend",
    "standardizer"
  ],
  "connectors": {
    "left": true,
    "right": true
  }
}
