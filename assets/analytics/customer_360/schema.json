{
  "component_type": "dagster_component_templates.Customer360Component",
  "description": "Component for creating unified customer profiles (Customer 360 view).",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of the unified customer profile asset",
      "required": true
    },
    "stripe_customers_asset": {
      "type": "string",
      "label": "Stripe Customers Asset",
      "description": "Stripe Customers Asset",
      "required": false,
      "default": null
    },
    "marketing_data_asset": {
      "type": "string",
      "label": "Marketing Data Asset",
      "description": "Marketing Data Asset",
      "required": false,
      "default": null
    },
    "ga4_data_asset": {
      "type": "string",
      "label": "Ga4 Data Asset",
      "description": "Ga4 Data Asset",
      "required": false,
      "default": null
    },
    "crm_data_asset": {
      "type": "string",
      "label": "Crm Data Asset",
      "description": "Crm Data Asset",
      "required": false,
      "default": null
    },
    "join_key": {
      "type": "string",
      "label": "Join Key",
      "description": "Join Key",
      "required": false,
      "default": "email"
    },
    "secondary_join_keys": {
      "type": "string",
      "label": "Secondary Join Keys",
      "description": "Secondary Join Keys",
      "required": false,
      "default": null
    },
    "active_days_threshold": {
      "type": "integer",
      "label": "Active Days Threshold",
      "description": "Active Days Threshold",
      "required": false,
      "default": 30
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": null,
      "ui:widget": "code"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group for organization",
      "required": false,
      "default": "customer_analytics"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "include_sample_metadata": {
      "type": "boolean",
      "label": "Include Sample Metadata",
      "description": "Include sample data preview in metadata",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    }
  },
  "tags": [
    "analytics",
    "customer",
    "360"
  ]
}
