type: dagster_component_templates.LakeFSAssetComponent
attributes:
  asset_name: data_version_commit
  repository: my-data-lake
  mode: commit
  branch: main
  commit_message: "Daily ETL materialization - {run_id}"
  endpoint: "https://lakefs.example.com"
  access_key_env_var: LAKEFS_ACCESS_KEY_ID
  secret_key_env_var: LAKEFS_SECRET_ACCESS_KEY
  group_name: data_versioning
  deps:
    - marts/orders
    - marts/customers
