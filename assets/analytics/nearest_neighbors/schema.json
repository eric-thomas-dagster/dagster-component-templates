{
  "component_type": "dagster_component_templates.NearestNeighborsComponent",
  "description": "For each record, find the K nearest neighbors by feature similarity.",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "feature_columns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Feature Columns",
      "description": "List of column names to use for distance computation",
      "required": true,
      "ui:widget": "list"
    },
    "n_neighbors": {
      "type": "integer",
      "label": "N Neighbors",
      "description": "Number of nearest neighbors to find per record",
      "required": false,
      "default": 5
    },
    "algorithm": {
      "type": "string",
      "label": "Algorithm",
      "description": "Algorithm: ",
      "required": false,
      "default": "auto"
    },
    "metric": {
      "type": "string",
      "label": "Metric",
      "description": "Distance metric (e.g. ",
      "required": false,
      "default": "euclidean"
    },
    "normalize": {
      "type": "boolean",
      "label": "Normalize",
      "description": "Standardize features with StandardScaler before computing distances",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "output_distances": {
      "type": "boolean",
      "label": "Output Distances",
      "description": "Add neighbor_N_dist columns with distances",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "output_indices": {
      "type": "boolean",
      "label": "Output Indices",
      "description": "Add neighbor_N_idx columns with row indices",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "analytics",
    "nearest",
    "neighbors"
  ]
}
