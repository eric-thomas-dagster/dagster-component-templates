{
  "component_type": "dagster_component_templates.PgvectorAssetComponent",
  "description": "Generate text embeddings via OpenAI and upsert them into a PostgreSQL table",
  "category": "analytics",
  "icon": "si:postgresql",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Dagster asset key for this component.",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream Asset Key",
      "required": true
    },
    "id_column": {
      "type": "string",
      "label": "Id Column",
      "description": "Primary key column in the upstream DataFrame. Used as the embedding row ID.",
      "required": true,
      "x-dagster-widget": "column"
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column containing the raw text to embed.",
      "required": true,
      "ui:widget": "textarea",
      "x-dagster-widget": "column"
    },
    "database_url_env_var": {
      "type": "string",
      "label": "Database Url Env Var",
      "description": "Database Url Env Var",
      "required": true
    },
    "target_table": {
      "type": "string",
      "label": "Target Table",
      "description": "Target Table",
      "required": true
    },
    "if_exists": {
      "type": "string",
      "label": "If Exists",
      "description": "If Exists",
      "required": false,
      "default": "upsert",
      "enum": [
        "fail",
        "replace",
        "append"
      ],
      "ui:widget": "select"
    },
    "precomputed_embedding_column": {
      "type": "string",
      "label": "Precomputed Embedding Column",
      "description": "Column in the upstream DataFrame that already contains embedding vectors (list[float] per row). When set, skips the embedder entirely — compose with any upstream embedding component (litellm_embedding_batch, voyage_embedding_batch, etc.).",
      "required": false
    },
    "embedding_model": {
      "type": "string",
      "label": "Embedding Model",
      "description": "LiteLLM model string for the inline-embedding path (used when precomputed_embedding_column is unset). Any LiteLLM-supported model: text-embedding-3-small (OpenAI), voyage/voyage-3, cohere/embed-english-v3.0, ollama/nomic-embed-text (LOCAL), huggingface/tei/…",
      "required": false,
      "default": "text-embedding-3-small"
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "description": "Env var containing the API key for the chosen embedding provider (OPENAI_API_KEY / VOYAGE_API_KEY / COHERE_API_KEY / etc.). Not required for local Ollama or when using precomputed_embedding_column. Alias openai_api_key_env_var also accepted.",
      "required": false
    },
    "openai_api_key_env_var": {
      "type": "string",
      "label": "OpenAI API Key Env Var (deprecated alias)",
      "description": "Deprecated alias for api_key_env_var — kept for backwards compatibility with pre-1.2.0 configs.",
      "required": false
    },
    "api_base_env_var": {
      "type": "string",
      "label": "API Base URL Env Var",
      "description": "Optional env var containing the API base URL — required for self-hosted providers like Ollama (e.g. http://localhost:11434) or Azure OpenAI. Forwarded to LiteLLM as api_base.",
      "required": false
    },
    "dimensions": {
      "type": "integer",
      "label": "Dimensions",
      "description": "Vector dimensionality. Must match the chosen model's output size (1536 for text-embedding-3-small, 3072 for text-embedding-3-large, 1024 for voyage-3, 768 for nomic-embed-text, etc.). Also becomes the pgvector column type — vector(<dimensions>).",
      "required": false,
      "default": 1536
    },
    "batch_size": {
      "type": "integer",
      "label": "Batch Size",
      "description": "Number of texts per embedding API request (inline path only).",
      "required": false,
      "default": 100
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name.",
      "required": false,
      "default": "vector_store"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null,
      "x-dagster-widget": "cron"
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "include_preview_metadata": {
      "type": "boolean",
      "label": "Include Preview Metadata",
      "required": false,
      "default": false
    },
    "preview_rows": {
      "type": "integer",
      "label": "Preview Rows",
      "required": false,
      "default": 25
    },
    "retry_policy_max_retries": {
      "type": "integer",
      "label": "Retry Policy Max Retries",
      "required": false
    },
    "retry_policy_delay_seconds": {
      "type": "integer",
      "label": "Retry Policy Delay Seconds",
      "required": false
    },
    "retry_policy_backoff": {
      "type": "string",
      "label": "Retry Policy Backoff",
      "required": false,
      "default": "exponential",
      "enum": [
        "exponential",
        "linear",
        "fixed"
      ],
      "ui:widget": "select"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "required": false
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "required": false
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "description": "Name for DynamicPartitionsDefinition (when partition_type='dynamic'), e.g. 'tenants'.",
      "required": false,
      "default": null
    },
    "partition_dimensions": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "label": "Partition Dimensions",
      "description": "Multi-axis partition spec: list of {name, type, start, values, dynamic_partition_name} dicts. Overrides flat fields when set.",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "analytics",
    "pgvector",
    "asset"
  ],
  "name": "pgvector Asset"
}
