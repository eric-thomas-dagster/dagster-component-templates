{
  "component_type": "dagster_component_templates.ReverseGeocoderComponent",
  "description": "Reverse geocode lat/lng coordinates to addresses in a DataFrame.",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame with lat/lng columns",
      "required": true
    },
    "lat_column": {
      "type": "string",
      "label": "Lat Column",
      "description": "Column name containing latitude values",
      "required": false,
      "default": "latitude"
    },
    "lng_column": {
      "type": "string",
      "label": "Lng Column",
      "description": "Column name containing longitude values",
      "required": false,
      "default": "longitude"
    },
    "provider": {
      "type": "string",
      "label": "Provider",
      "description": "Geocoding provider: ",
      "required": false,
      "default": "nominatim"
    },
    "api_key_env_var": {
      "type": "string",
      "label": "Api Key Env Var",
      "description": "Api Key Env Var",
      "required": false,
      "default": null
    },
    "user_agent": {
      "type": "string",
      "label": "User Agent",
      "description": "User agent string required for Nominatim requests",
      "required": false,
      "default": "dagster_reverse_geocoder"
    },
    "output_address_column": {
      "type": "string",
      "label": "Output Address Column",
      "description": "Column name for the full resolved address",
      "required": false,
      "default": "address"
    },
    "output_city_column": {
      "type": "string",
      "label": "Output City Column",
      "description": "Optional column name for the city name",
      "required": false,
      "default": null
    },
    "output_country_column": {
      "type": "string",
      "label": "Output Country Column",
      "description": "Optional column name for the country name",
      "required": false,
      "default": null
    },
    "timeout": {
      "type": "integer",
      "label": "Timeout",
      "description": "Request timeout in seconds",
      "required": false,
      "default": 10
    },
    "batch_delay": {
      "type": "number",
      "label": "Batch Delay",
      "description": "Seconds to wait between requests to respect rate limits",
      "required": false,
      "default": 1.0
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    }
  },
  "tags": [
    "analytics",
    "reverse",
    "geocoder"
  ],
  "name": "Reverse Geocoder"
}