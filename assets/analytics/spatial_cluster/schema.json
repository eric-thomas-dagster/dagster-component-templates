{
  "component_type": "dagster_component_templates.SpatialClusterComponent",
  "description": "Cluster geographic points using DBSCAN or K-means.",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame with lat/lng columns",
      "required": true
    },
    "lat_column": {
      "type": "string",
      "label": "Lat Column",
      "description": "Column name containing latitude values",
      "required": false,
      "default": "latitude"
    },
    "lng_column": {
      "type": "string",
      "label": "Lng Column",
      "description": "Column name containing longitude values",
      "required": false,
      "default": "longitude"
    },
    "algorithm": {
      "type": "string",
      "label": "Algorithm",
      "description": "Clustering algorithm: ",
      "required": false,
      "default": "dbscan"
    },
    "eps_km": {
      "type": "number",
      "label": "Eps Km",
      "description": "DBSCAN only: maximum distance in km between neighborhood points",
      "required": false,
      "default": 1.0
    },
    "min_samples": {
      "type": "integer",
      "label": "Min Samples",
      "description": "DBSCAN only: minimum number of points to form a dense region",
      "required": false,
      "default": 3
    },
    "n_clusters": {
      "type": "integer",
      "label": "N Clusters",
      "description": "K-means only: number of clusters to form",
      "required": false,
      "default": 5
    },
    "output_column": {
      "type": "string",
      "label": "Output Column",
      "description": "Output Column",
      "required": false,
      "default": "spatial_cluster"
    },
    "include_cluster_center": {
      "type": "boolean",
      "label": "Include Cluster Center",
      "description": "Include Cluster Center",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "analytics",
    "spatial",
    "cluster"
  ]
}
