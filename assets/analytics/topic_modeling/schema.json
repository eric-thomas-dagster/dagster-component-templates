{
  "component_type": "dagster_component_templates.TopicModelingComponent",
  "description": "Discover topics in a text column using Latent Dirichlet Allocation (LDA).",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "text_column": {
      "type": "string",
      "label": "Text Column",
      "description": "Column name containing text to model",
      "required": true,
      "ui:widget": "textarea"
    },
    "n_topics": {
      "type": "integer",
      "label": "N Topics",
      "description": "Number of topics to discover",
      "required": false,
      "default": 10
    },
    "n_top_words": {
      "type": "integer",
      "label": "N Top Words",
      "description": "Number of top words per topic to log to metadata",
      "required": false,
      "default": 10
    },
    "max_features": {
      "type": "integer",
      "label": "Max Features",
      "description": "Maximum vocabulary size for CountVectorizer",
      "required": false,
      "default": 1000
    },
    "max_iter": {
      "type": "integer",
      "label": "Max Iter",
      "description": "Maximum LDA iterations",
      "required": false,
      "default": 10
    },
    "random_state": {
      "type": "integer",
      "label": "Random State",
      "description": "Random seed for reproducibility",
      "required": false,
      "default": 42
    },
    "output_mode": {
      "type": "string",
      "label": "Output Mode",
      "description": "Output mode: ",
      "required": false,
      "default": "dominant_topic"
    },
    "language": {
      "type": "string",
      "label": "Language",
      "description": "Language for CountVectorizer stop words",
      "required": false,
      "default": "english"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "analytics",
    "topic",
    "modeling"
  ]
}
