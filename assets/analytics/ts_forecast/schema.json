{
  "component_type": "dagster_component_templates.TsForecastComponent",
  "description": "Component for generating time series forecasts using ARIMA or ETS models.",
  "category": "analytics",
  "icon": "BarChart2",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Analytics result DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame with date + value columns",
      "required": true
    },
    "date_column": {
      "type": "string",
      "label": "Date Column",
      "description": "Column containing dates or timestamps",
      "required": true
    },
    "value_column": {
      "type": "string",
      "label": "Value Column",
      "description": "Column containing the numeric time series values",
      "required": true
    },
    "forecast_periods": {
      "type": "integer",
      "label": "Forecast Periods",
      "description": "Number of future periods to forecast",
      "required": false,
      "default": 12
    },
    "model": {
      "type": "string",
      "label": "Model",
      "description": "Model to use: ",
      "required": false,
      "default": "auto"
    },
    "arima_order": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "label": "Arima Order",
      "description": "Arima Order",
      "required": false,
      "default": "[1",
      "ui:widget": "list"
    },
    "ets_trend": {
      "type": "string",
      "label": "Ets Trend",
      "description": "ETS trend component: None, ",
      "required": false,
      "default": "add"
    },
    "ets_seasonal": {
      "type": "string",
      "label": "Ets Seasonal",
      "description": "ETS seasonal component: None, ",
      "required": false,
      "default": "add"
    },
    "ets_seasonal_periods": {
      "type": "integer",
      "label": "Ets Seasonal Periods",
      "description": "Number of periods in one seasonal cycle",
      "required": false,
      "default": 12
    },
    "output_mode": {
      "type": "string",
      "label": "Output Mode",
      "description": "Output mode: ",
      "required": false,
      "default": "forecast_only"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "analytics",
    "forecast"
  ]
}
