{
  "type": "dagster_component_templates.graphql_asset",
  "name": "GraphQL Asset",
  "category": "assets",
  "description": "Executes one or more GraphQL queries against an API, flattens responses with pandas, and writes results to a destination database via SQLAlchemy with optional cursor-based or page-based pagination",
  "dependencies": {
    "pip": ["requests>=2.31.0", "pandas>=1.3.0", "sqlalchemy>=2.0.0"]
  },
  "tags": ["assets", "graphql", "api", "ingestion", "sql", "pagination"]
}
