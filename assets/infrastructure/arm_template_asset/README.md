# ArmTemplateAssetComponent

Deploy an Azure Resource Manager (ARM) template as a Dagster asset.

## Dependencies
- `azure-mgmt-resource`
- `azure-identity`

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_name` | `str` | Dagster asset name for this ARM deployment |
| `resource_group` | `str` | Azure resource group to deploy into |
| `deployment_name` | `str` | ARM deployment name (idempotent — re-runs update in place) |

### Execution

| Field | Type | Default | Description |
|---|---|---|---|
| `parameters` | `dict` | — | Inline parameter overrides |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `description` | `str` | — | — |
| `group_name` | `str` | `"infrastructure"` | — |
| `deps` | `list[str]` | — | — |
| `owners` | `list[str]` | — | — |
| `asset_tags` | `dict` | — | — |
| `kinds` | `list[str]` | — | — |

### Retry policy

| Field | Type | Default | Description |
|---|---|---|---|
| `retry_policy_max_retries` | `int` | — | — |
| `retry_policy_delay_seconds` | `int` | — | — |
| `retry_policy_backoff` | `str` | `"exponential"` | — |

### Source / target

| Field | Type | Default | Description |
|---|---|---|---|
| `mode` | `str` | `"Incremental"` | Deployment mode: 'Incremental' or 'Complete' |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `template_file` | `str` | — | Local path to ARM JSON template |
| `template_uri` | `str` | — | HTTPS URL of remote ARM template (mutually exclusive with template_file) |
| `parameters_file` | `str` | — | Local path to parameters JSON file |
| `subscription_id` | `str` | — | Azure subscription ID (defaults to AZURE_SUBSCRIPTION_ID env) |
| `location` | `str` | — | Azure region (only required for subscription-scope deployments) |
| `what_if` | `bool` | `false` | Run a what-if preview only (no changes applied) |

[//]: # (FIELDS:END)

## Authentication
Reads from environment variables — see the `*_env` fields in `component.py`.

## See also
- [Schema](schema.json)
- [Example config](example.yaml)