{
  "component_type": "dagster_component_templates.DockerContainerAssetComponent",
  "description": "Run a Docker container as a Dagster asset via dagster-docker.",
  "category": "infrastructure",
  "icon": "Box",
  "x-dagster-io": {
    "outputs": {
      "type": "infrastructure",
      "description": "Run a Docker container as a Dagster asset via dagster-docker."
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "required": true
    },
    "image": {
      "type": "string",
      "label": "Image",
      "required": true
    },
    "command": {
      "type": "array",
      "label": "Command",
      "required": false,
      "items": {
        "type": "string"
      }
    },
    "env_vars": {
      "type": "object",
      "label": "Env Vars",
      "required": false
    },
    "network": {
      "type": "string",
      "label": "Network",
      "required": false
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": true
    },
    "deps": {
      "type": "array",
      "label": "Deps",
      "required": false,
      "items": {
        "type": "string"
      }
    }
  },
  "tags": [
    "infrastructure",
    "docker",
    "container",
    "official"
  ],
  "name": "DockerContainerAsset"
}
