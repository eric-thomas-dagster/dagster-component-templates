# `DryRunAssetComponent` + `@dry_run` decorator

Run the compute, skip the write. Ideal for validating a production asset's compute logic without dirtying downstreams.

## What it does

Executes the asset's compute function. On dry-run mode:

- The **return value is discarded** (IO manager not invoked).
- Asset yields `MaterializeResult(metadata={dry_run: true, elapsed_seconds, would_produce_bytes})`.
- Emits `AssetObservation` tagged `dry_run=true`.
- Downstreams read via IO manager still see the last non-dry-run value.

## Enable modes

Priority order (highest wins):

1. Explicit `dry_run(enabled=True)` decorator arg / `enabled: true` YAML.
2. Run tag `dry_run` in `("true", "1", "yes")`.
3. Env var `DAGSTER_DRY_RUN` in `("true", "1", "yes")`.
4. Default: disabled.

## Why this belongs in Dagster

- **Run-tag enable** — flip dry-run on a single run from the UI/CLI without redeploy.
- **AssetObservation with dry_run tag** — every dry run is a queryable event.
- **Materialization result surfaces the mode** — Dagit shows `dry_run: True` prominently on that materialization.
- **Composes with the rest of the decorator stack** — dry runs still respect `@sla`, `@throttle`, `@smart_retry`, `@lifecycle` audits.

## Full YAML example

```yaml
type: dagster_community_components.DryRunAssetComponent
attributes:
  asset_name: costly_report

  compute:
    kind: python
    python: "my_project.reports:build_costly"

  # enabled: null   # (default) read run tag `dry_run` or env DAGSTER_DRY_RUN
```

## `@dry_run` decorator

```python
import dagster as dg
from dagster_community_components import dry_run

@dg.asset
@dry_run()  # enable via run tag `dry_run=true` or env DAGSTER_DRY_RUN=1
def costly_report(context):
    return build_report()
```

Launch a dry run from CLI:

```bash
dg launch --assets costly_report --tag dry_run=true
```

Or from Python:

```python
dg.materialize([costly_report], tags={"dry_run": "true"})
```

## Composes with

- **`@sla`** — SLA still fires; useful for validating duration regressions.
- **`@throttle`** — throttled dry runs still emit `throttle_skipped` observations.
- **`@smart_retry`** — retries within a dry run.
- **`@lifecycle`** — dry-run mode skips publish; audit still runs.
- **`@profile`** — profile a candidate output before committing.

## Optional hooks

### Cost estimate

Attach a cost model to see what the run **would** cost without paying. `cost_fn` is a `'mod:fn'` reference to `(context, elapsed_s, result) -> usd`.

```yaml
type: dagster_community_components.DryRunAssetComponent
attributes:
  asset_name: llm_summary
  compute:
    kind: python
    python: "my_project.summaries:generate"
  cost_fn: "my_project.pricing:llm_cost"
```

Python:

```python
def llm_cost(context, elapsed_s, result) -> float:
    return result.get("tokens", 0) * 0.000002  # $2 / 1M tokens

@dg.asset
@dry_run(enabled=True, cost_fn="my_project.pricing:llm_cost")
def llm_summary(context):
    return call_openai(...)
```

The dry-run observation emits `estimated_cost_usd` alongside `elapsed_seconds`.

### Diff vs. current committed value

Compare the discarded output against the last committed materialization. Emits `dry_run_diff` metadata: `row_count_delta`, `column_delta` (added/removed), `sample_value_diffs` on the first N cols.

```yaml
type: dagster_community_components.DryRunAssetComponent
attributes:
  asset_name: order_totals
  compute:
    kind: python
    python: "my_project.reports:order_totals"
  diff_vs_current: true
```

Silent no-op if no prior materialization exists. In `wraps:` mode with either `cost_fn` or `diff_vs_current` set, the inner compute WILL run (both hooks need a candidate value); without them, the inner is short-circuited (stronger dry-run).

## CLI demos using this template

| Demo | Setup script | What it shows |
|---|---|---|
| [`dry_run_asset.md`](https://github.com/eric-thomas-dagster/dagster-community-components-cli/blob/main/examples/dry_run_asset.md) | [`setup_dry_run_asset_demo.sh`](https://github.com/eric-thomas-dagster/dagster-community-components-cli/blob/main/examples/setup_dry_run_asset_demo.sh) | 100% offline. Demonstrates BOTH shapes AND a semantic difference: (1) `@dry_run` Python decorator RUNS the compute + discards the return value, (2) `DryRunAssetComponent { enabled: true, wraps: SyntheticDataGeneratorComponent }` — inner compute is NEVER INVOKED (stronger dry-run, prevents inner's side effects too). Includes a control run with `enabled: false` proving passthrough. Enable via decorator arg, YAML field, run tag `dry_run=true`, or env `DAGSTER_DRY_RUN=1`. |

```bash
curl -fsSL https://raw.githubusercontent.com/eric-thomas-dagster/dagster-community-components-cli/main/examples/setup_dry_run_asset_demo.sh | bash
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## What's not in v1 (roadmap)

- **Per-partition dry-run** — mask specific partitions from a dry run while committing others.

## Fields

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `asset_name` | `str` | — | Dagster asset name. Required when NOT using `wraps:` (inherited from inner in wraps mode). |
| `group_name` | `str` | — | — |
| `description` | `str` | — | — |
| `owners` | `List[str]` | — | — |
| `tags` | `Dict[str, str]` | — | — |
| `kinds` | `List[str]` | — | Default: ['python', 'dry_run']. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `upstream_asset_key` | `str` | — | — |
| `compute` | `Dict[str, Any]` | — | `{kind: python, python: 'mod:fn'}`. Mutually exclusive with `wraps`. |
| `wraps` | `Dict[str, Any]` | — | Wrap another DCC component's assets with dry-run short-circuit behavior instead of defining new compute. Shape: `{type: 'dagster_community_components.<Component>', attributes: {...}}`. Mutually exclusive with `compute`. |
| `enabled` | `bool` | — | Explicit dry-run override. When null (default), reads run tag `dry_run` in ('true','1','yes'), else env `DAGSTER_DRY_RUN`. |
| `cost_fn` | `str` | — | Optional 'mod:fn' reference to a callable `(context, elapsed_s, result) -> usd`. When set, the dry-run observation surfaces `estimated_cost_usd` — see what the run would cost without paying. |
| `diff_vs_current` | `bool` | `false` | When True and dry-run is enabled, compare the discarded output against the latest committed asset value. Emits `dry_run_diff` observation metadata (row_count_delta, column_delta, sample_value_diffs). Silent no-op if no p… _(full docs in schema.json + component README)_ |

[//]: # (FIELDS:END)
