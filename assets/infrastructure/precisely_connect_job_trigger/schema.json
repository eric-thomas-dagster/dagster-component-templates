{
  "component_type": "dagster_component_templates.PreciselyConnectJobTriggerComponent",
  "description": "Trigger a Precisely Connect ETL job-run via the ``/runtask`` endpoint.\n\nExample:\n    ```yaml\n    type: dagster_community_components.PreciselyConnectJobTriggerComponent\n    attributes:\n      asset_name: precisely_load_customers_run\n      job_id: \"abc-123-xyz\"\n      host_env_var: PRECISELY_HOST\n      api_token_env_var: PRECISELY_API_TOKEN\n      parameters:\n        INPUT_FILE: /data/customers.csv\n        BATCH_DATE: \"2026-05-27\"\n    ```\n\nThe returned jobRunId is captured as asset metadata so a paired\n``precisely_job_sensor`` can read it from the latest materialization\nand watch for terminal SUCCESS — closing the submit + watch loop.",
  "category": "infrastructure",
  "x-dagster-io": {
    "outputs": {
      "type": "MaterializeResult"
    }
  },
  "icon": "PlayCircle",
  "attributes": {
    "asset_key": {
      "type": "string",
      "label": "Asset Key",
      "required": true,
      "description": "Dagster asset key for the Precisely job (e.g. 'precisely/etl/load_customers'). Use '/' separators for nested keys. Same value an ``external_precisely_job`` would use — but do NOT use both components on the same key; pick the Case (A or B in the module docstring) that matches your schedule ownership."
    },
    "job_id": {
      "type": "string",
      "label": "Job ID",
      "required": true,
      "description": "Precisely Connect ETL stable job id (the job to trigger)."
    },
    "host_env_var": {
      "type": "string",
      "label": "Host Env Var",
      "required": false,
      "description": "Env var with Precisely Connect ETL host URL.",
      "default": "PRECISELY_HOST"
    },
    "api_token_env_var": {
      "type": "string",
      "label": "API Token Env Var",
      "required": false,
      "description": "Env var with Precisely API bearer token.",
      "default": "PRECISELY_API_TOKEN"
    },
    "parameters": {
      "type": "object",
      "label": "Parameters",
      "required": false,
      "description": "Optional Connect ETL task parameters passed as JSON body to /runtask. Shape is job-specific (defined in DMExpress / Connect ETL UI); examples include INPUT_FILE, BATCH_DATE, etc.",
      "default": null
    },
    "timeout_seconds": {
      "type": "integer",
      "label": "Timeout Seconds",
      "required": false,
      "description": "HTTP timeout for the submit POST.",
      "default": 60
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": false,
      "default": null,
      "description": "Dagster asset group name"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "required": false
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "required": false,
      "description": "Asset owners — team names (team:analytics) or email addresses.",
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "required": false,
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "required": false,
      "description": "Asset kinds shown in the Dagster catalog. Auto-inferred from component name if empty.",
      "ui:widget": "list"
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "required": false
    }
  }
}
