{
  "component_type": "dagster_community_components.PrefectBackgroundTaskAssetComponent",
  "description": "Submit a Prefect background task (.delay()) as a Dagster asset. Requires a task worker serving the task. task_import_path must be importable from wherever this component runs — background tasks have no by-name trigger API.",
  "category": "infrastructure",
  "icon": "si:prefect",
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "required": true,
      "description": "Dagster asset name (or '/' path)."
    },
    "task_import_path": {
      "type": "string",
      "label": "Task Import Path",
      "required": true,
      "description": "'module.path:function_name' of the @task-decorated function to submit."
    },
    "parameters": {
      "type": "object",
      "label": "Parameters",
      "required": false,
      "description": "Keyword args passed to task.delay(**parameters). Templated: {partition_key}, {run_id}, {partition_window_start}, {partition_window_end}.",
      "default": null
    },
    "wait_for_result": {
      "type": "boolean",
      "label": "Wait For Result",
      "required": false,
      "description": "Wait for task completion before materializing.",
      "default": true
    },
    "timeout_seconds": {
      "type": "integer",
      "label": "Timeout Seconds",
      "required": false,
      "description": "Only when wait_for_result=True. Max seconds to wait. None=indefinite.",
      "default": null
    },
    "poll_interval_seconds": {
      "type": "number",
      "label": "Poll Interval Seconds",
      "required": false,
      "description": "Only when wait_for_result=True. Seconds between polls.",
      "default": 5.0
    },
    "api_url": {
      "type": "string",
      "label": "API URL",
      "required": false,
      "description": "Prefect API URL.",
      "default": "http://127.0.0.1:4200/api"
    },
    "api_key_env_var": {
      "type": "string",
      "label": "API Key Env Var",
      "required": false,
      "description": "Env var holding a Prefect Cloud API key.",
      "default": null
    },
    "ui_url": {
      "type": "string",
      "label": "UI URL",
      "required": false,
      "description": "Prefect UI base URL, for the Prefect Run URL metadata link. Defaults to api_url minus '/api'.",
      "default": null
    },
    "fail_on_task_run_failure": {
      "type": "boolean",
      "label": "Fail On Task Run Failure",
      "required": false,
      "description": "Fail the Dagster asset if the Prefect task run ends in a non-COMPLETED state. Ignored in execution_mode='pipes'.",
      "default": true
    },
    "stream_logs": {
      "type": "boolean",
      "label": "Stream Logs",
      "required": false,
      "description": "Forward the task's own Prefect logs into the Dagster run log while waiting. execution_mode='poll' only.",
      "default": false
    },
    "stream_artifacts": {
      "type": "boolean",
      "label": "Stream Artifacts",
      "required": false,
      "description": "Forward Prefect artifacts the task creates as AssetObservation events (or AssetCheckResults, via check_names). execution_mode='poll' only.",
      "default": false
    },
    "check_names": {
      "type": "array",
      "label": "Check Names",
      "required": false,
      "description": "Only with stream_artifacts=true. Table artifact keys matching these become AssetCheckResults.",
      "default": null,
      "items": {
        "type": "string"
      }
    },
    "execution_mode": {
      "type": "string",
      "label": "Execution Mode",
      "required": false,
      "description": "'poll' — plain Prefect SDK, no task code changes. 'pipes' — official dagster-prefect PipesPrefectTaskClient (requires pip install dagster-prefect + a dagster_pipes_params argument on the task).",
      "default": "poll",
      "enum": ["poll", "pipes"],
      "ui:widget": "select"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": false,
      "default": null
    },
    "description": {
      "type": "string",
      "label": "Description",
      "required": false,
      "default": null
    },
    "owners": {
      "type": "array",
      "label": "Owners",
      "required": false,
      "default": null,
      "items": {
        "type": "string"
      }
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "required": false,
      "default": null,
      "additionalProperties": {
        "type": "string"
      }
    },
    "kinds": {
      "type": "array",
      "label": "Kinds",
      "required": false,
      "description": "Asset kinds. Defaults to ['prefect'].",
      "default": null,
      "items": {
        "type": "string"
      }
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag Minutes",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "dynamic",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "required": false,
      "default": null
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "required": false,
      "default": null
    },
    "partition_dimensions": {
      "type": "array",
      "label": "Partition Dimensions",
      "required": false,
      "default": null,
      "items": {
        "type": "object"
      }
    },
    "deps": {
      "type": "array",
      "label": "Deps",
      "required": false,
      "description": "Lineage-only upstream asset keys.",
      "default": null,
      "items": {
        "type": "string"
      }
    },
    "retry_policy_max_retries": {
      "type": "integer",
      "label": "Retry Max Retries",
      "required": false,
      "default": null
    },
    "retry_policy_delay_seconds": {
      "type": "integer",
      "label": "Retry Delay Seconds",
      "required": false,
      "default": null
    },
    "retry_policy_backoff": {
      "type": "string",
      "label": "Retry Backoff",
      "required": false,
      "default": "exponential"
    }
  }
}
