{
  "component_type": "dagster_component_templates.DocuSignIngestionComponent",
  "description": "Ingest DocuSign envelope and template data using dlt's generic REST API source.",
  "category": "ingestion",
  "vertical": "productivity",
  "icon": "si:docusign",
  "x-dagster-io": {
    "outputs": {
      "type": "dataframe",
      "description": "pandas DataFrame with one row per DocuSign record (or MaterializeResult if destination is non-SQL / persist_only=true)."
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of the asset that will hold the data",
      "required": true
    },
    "integration_key": {
      "type": "string",
      "label": "Integration Key",
      "description": "DocuSign Integration Key (client ID) with an RSA keypair registered for JWT grant.",
      "required": true
    },
    "user_id": {
      "type": "string",
      "label": "User Id",
      "description": "GUID of the DocuSign user being impersonated (must have granted one-time JWT consent).",
      "required": true
    },
    "private_key": {
      "type": "string",
      "label": "Private Key",
      "description": "RSA private key (PEM format) matching the public key registered on the Integration Key.",
      "required": true
    },
    "account_id": {
      "type": "string",
      "label": "Account Id",
      "description": "Specific DocuSign account ID to use. If unset, the default account from the JWT userinfo response is used.",
      "required": false,
      "default": null
    },
    "use_demo_env": {
      "type": "boolean",
      "label": "Use Demo Env",
      "description": "Use DocuSign's demo/sandbox environment (account-d.docusign.com) instead of production.",
      "required": false,
      "default": null
    },
    "resources": {
      "type": "string",
      "label": "Resources",
      "description": "Comma-separated list of resources to extract: envelopes, templates, users",
      "required": false,
      "default": "envelopes"
    },
    "destination": {
      "type": "string",
      "label": "Destination",
      "description": "dlt destination identifier. Leave empty for in-memory DuckDB -> DataFrame mode.",
      "required": false,
      "default": null
    },
    "dataset_name": {
      "type": "string",
      "label": "Dataset Name",
      "description": "Target dataset/schema in the destination. Defaults to the asset name.",
      "required": false,
      "default": null
    },
    "persist_only": {
      "type": "boolean",
      "label": "Persist Only",
      "description": "If True with destination set: emit a MaterializeResult and skip DataFrame return.",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "destination_credentials_url": {
      "type": "string",
      "label": "Destination Credentials Url",
      "description": "Inline connection string passed to dlt's destination factory.",
      "required": false,
      "default": null
    },
    "destination_credentials_env_var": {
      "type": "string",
      "label": "Destination Credentials Env Var",
      "description": "Env var holding the destination connection string.",
      "required": false,
      "default": null
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group for organization",
      "required": false,
      "default": "docusign_ingestion"
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners",
      "required": false,
      "default": null
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Additional key-value tags",
      "required": false,
      "default": null
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds for the catalog",
      "required": false,
      "default": null
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag Minutes",
      "description": "Maximum acceptable lag in minutes.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron",
      "description": "Cron schedule string for the freshness policy.",
      "required": false,
      "default": null
    },
    "include_preview_metadata": {
      "type": "boolean",
      "label": "Include Preview Metadata",
      "description": "Include sample data preview in metadata",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "preview_rows": {
      "type": "integer",
      "label": "Preview Rows",
      "description": "Rows to include in the preview metadata.",
      "required": false,
      "default": 25
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "description": "Upstream asset keys this asset depends on",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Partition type: 'daily' / 'weekly' / 'monthly' / 'hourly' / 'static' / 'multi' / 'dynamic' / None. Note: the envelopes resource is genuinely bound to the partition window via from_date/to_date params (see context.partition_time_window in component.py). templates and users are snapshot-style and are NOT bound -- every run re-fetches them in full regardless of partition.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi",
        "dynamic"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start",
      "description": "Partition start date in ISO format. Required for time-based partition types.",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning.",
      "required": false,
      "default": null
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "description": "Name for DynamicPartitionsDefinition.",
      "required": false,
      "default": null
    },
    "partition_dimensions": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "label": "Partition Dimensions",
      "description": "Multi-axis partition spec.",
      "required": false,
      "default": null
    },
    "retry_policy_max_retries": {
      "type": "integer",
      "label": "Retry Policy Max Retries",
      "description": "Max retries on asset failure.",
      "required": false,
      "default": null
    },
    "retry_policy_delay_seconds": {
      "type": "integer",
      "label": "Retry Policy Delay Seconds",
      "description": "Seconds between retries.",
      "required": false,
      "default": null
    },
    "retry_policy_backoff": {
      "type": "string",
      "label": "Retry Policy Backoff",
      "description": "Backoff strategy.",
      "required": false,
      "default": "exponential",
      "enum": [
        "exponential",
        "linear"
      ],
      "ui:widget": "select"
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage.",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ingestion",
    "docusign",
    "dlt",
    "rest_api"
  ],
  "name": "DocuSign Ingestion"
}
