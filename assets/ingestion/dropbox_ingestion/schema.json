{
  "component_type": "dagster_component_templates.DropboxIngestionComponent",
  "description": "Ingest Dropbox file/folder listings via a hand-written cursor-paginated fetch (not dlt's rest_api_source -- Dropbox's list_folder endpoint is POST+JSON-body, not GET+query-params).",
  "category": "ingestion",
  "vertical": "cloud-storage",
  "icon": "si:dropbox",
  "x-dagster-io": {
    "outputs": {
      "type": "dataframe",
      "description": "pandas DataFrame with one row per Dropbox file/folder entry (or MaterializeResult if destination is non-SQL / persist_only=true)."
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of the asset that will hold the data",
      "required": true
    },
    "client_id": {
      "type": "string",
      "label": "Client Id",
      "description": "Dropbox app client ID (used for the OAuth2 refresh-token exchange).",
      "required": true
    },
    "client_secret": {
      "type": "string",
      "label": "Client Secret",
      "description": "Dropbox app client secret.",
      "required": true
    },
    "refresh_token": {
      "type": "string",
      "label": "Refresh Token",
      "description": "OAuth2 refresh token obtained once via Dropbox's OAuth consent flow (token_access_type=offline). Dropbox deprecated long-lived static access tokens.",
      "required": true
    },
    "folder_path": {
      "type": "string",
      "label": "Folder Path",
      "description": "Dropbox folder path to list, e.g. '/Reports'. Empty string lists the root.",
      "required": false,
      "default": ""
    },
    "recursive": {
      "type": "boolean",
      "label": "Recursive",
      "description": "List all nested subfolders' contents too, not just the immediate folder.",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "resources": {
      "type": "string",
      "label": "Resources",
      "description": "Comma-separated list of resources to extract: files",
      "required": false,
      "default": "files"
    },
    "destination": {
      "type": "string",
      "label": "Destination",
      "description": "dlt destination identifier. Leave empty for in-memory DuckDB -> DataFrame mode.",
      "required": false,
      "default": null
    },
    "dataset_name": {
      "type": "string",
      "label": "Dataset Name",
      "description": "Target dataset/schema in the destination. Defaults to the asset name.",
      "required": false,
      "default": null
    },
    "persist_only": {
      "type": "boolean",
      "label": "Persist Only",
      "description": "If True with destination set: emit a MaterializeResult and skip DataFrame return.",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "destination_credentials_url": {
      "type": "string",
      "label": "Destination Credentials Url",
      "description": "Inline connection string passed to dlt's destination factory.",
      "required": false,
      "default": null
    },
    "destination_credentials_env_var": {
      "type": "string",
      "label": "Destination Credentials Env Var",
      "description": "Env var holding the destination connection string.",
      "required": false,
      "default": null
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group for organization",
      "required": false,
      "default": "dropbox_ingestion"
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners",
      "required": false,
      "default": null
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Additional key-value tags",
      "required": false,
      "default": null
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds for the catalog",
      "required": false,
      "default": null
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag Minutes",
      "description": "Maximum acceptable lag in minutes.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron",
      "description": "Cron schedule string for the freshness policy.",
      "required": false,
      "default": null
    },
    "include_preview_metadata": {
      "type": "boolean",
      "label": "Include Preview Metadata",
      "description": "Include sample data preview in metadata",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "preview_rows": {
      "type": "integer",
      "label": "Preview Rows",
      "description": "Rows to include in the preview metadata.",
      "required": false,
      "default": 25
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "description": "Upstream asset keys this asset depends on",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Partition type: 'daily' / 'weekly' / 'monthly' / 'hourly' / 'static' / 'multi' / 'dynamic' / None. Note: this connector's compute function does not use the partition key to filter the folder listing -- every partition run re-lists the same full folder tree. Declaring a partition type here controls the materialization/backfill schedule in Dagster, not what data is fetched.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi",
        "dynamic"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start",
      "description": "Partition start date in ISO format. Required for time-based partition types.",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning.",
      "required": false,
      "default": null
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "description": "Name for DynamicPartitionsDefinition.",
      "required": false,
      "default": null
    },
    "partition_dimensions": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "label": "Partition Dimensions",
      "description": "Multi-axis partition spec.",
      "required": false,
      "default": null
    },
    "retry_policy_max_retries": {
      "type": "integer",
      "label": "Retry Policy Max Retries",
      "description": "Max retries on asset failure.",
      "required": false,
      "default": null
    },
    "retry_policy_delay_seconds": {
      "type": "integer",
      "label": "Retry Policy Delay Seconds",
      "description": "Seconds between retries.",
      "required": false,
      "default": null
    },
    "retry_policy_backoff": {
      "type": "string",
      "label": "Retry Policy Backoff",
      "description": "Backoff strategy.",
      "required": false,
      "default": "exponential",
      "enum": [
        "exponential",
        "linear"
      ],
      "ui:widget": "select"
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage.",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "ingestion",
    "dropbox",
    "dlt",
    "cursor-paginated"
  ],
  "name": "Dropbox Ingestion"
}
