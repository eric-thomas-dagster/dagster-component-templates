# posthog_events_ingestion

Pull events from a PostHog project into a pandas DataFrame. Handles cursor pagination automatically.

## Prereqs

- Personal API key with `query:read` scope. Generate at Account Settings → Personal API Keys.
- Set the token in the env var referenced by `api_key_env_var` (default `POSTHOG_API_KEY`).

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_name` | `str` | Output asset name. |
| `project_id` | `str` | PostHog project ID (numeric). Find in your project settings. |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `api_key_env_var` | `str` | `"POSTHOG_API_KEY"` | Env var holding a PostHog personal API key with `query:read` scope. |

### Execution

| Field | Type | Default | Description |
|---|---|---|---|
| `limit` | `int` | `1000` | Total row cap. Paginates until this limit or all events fetched. |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `description` | `str` | — | — |
| `group_name` | `str` | `"posthog"` | — |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `api_host` | `str` | `"https://us.i.posthog.com"` | PostHog API host. Use 'https://us.i.posthog.com' (default) for US Cloud, 'https://eu.i.posthog.com' for EU Cloud, or your self-hosted URL. |
| `event` | `str` | — | Filter by event name (e.g. '$pageview'). None = all events. |
| `since_days` | `int` | `1` | Fetch events from the last N days (max 90). |
| `include_preview_metadata` | `bool` | `true` | — |
| `preview_rows` | `int` | `25` | — |

[//]: # (FIELDS:END)

## Output

DataFrame with columns from PostHog events (id, event, distinct_id, timestamp, properties.*, person.*, etc.). Nested fields flattened with `.` separator via `pd.json_normalize`.

## Related

- `posthog_persons_ingestion` — persons (identified users + traits)
- `sentry_issues_ingestion` — pair with Sentry for error-vs-behavior correlation
- `dataframe_to_snowflake` / `dataframe_to_bigquery` — persist events to your warehouse
