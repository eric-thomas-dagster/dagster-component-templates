# sentry_issues_ingestion

Pull issues from a Sentry project via the REST API into a pandas DataFrame. Uses `Link`-header cursor pagination.

## Prereqs

- Sentry auth token with `project:read` scope. Create at Organization Settings → Auth Tokens.
- Set the token in the env var referenced by `api_token_env_var` (default `SENTRY_AUTH_TOKEN`).

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_name` | `str` | Output asset name. |
| `organization` | `str` | Sentry organization slug (from the URL, e.g. 'acme-inc'). |
| `project` | `str` | Sentry project slug (from the URL, e.g. 'frontend-web'). |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `api_token_env_var` | `str` | `"SENTRY_AUTH_TOKEN"` | Env var holding the Sentry auth token (needs `project:read` scope). |

### Execution

| Field | Type | Default | Description |
|---|---|---|---|
| `limit` | `int` | `1000` | Total row cap across paginated pages. |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `description` | `str` | — | — |
| `group_name` | `str` | `"sentry"` | — |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `api_host` | `str` | `"https://sentry.io"` | Sentry host. Use 'https://sentry.io' (default) for SaaS or your self-hosted URL. |
| `query` | `str` | `"is:unresolved"` | Sentry search query. Examples: 'is:unresolved', 'is:resolved age:-24h', 'level:error'. See https://docs.sentry.io/product/sentry-basics/search/ |
| `stats_period` | `str` | `"24h"` | Time range for the query (e.g. '24h', '7d', '30d'). None = default. |
| `include_preview_metadata` | `bool` | `true` | — |
| `preview_rows` | `int` | `25` | — |

[//]: # (FIELDS:END)

## Output

DataFrame with columns like `id, shortId, title, culprit, level, status, count, userCount, firstSeen, lastSeen, metadata.*, project.*, permalink`. Nested fields flattened via `pd.json_normalize`.

## Related

- `dataframe_to_sentry` — send events / metrics TO Sentry
- `posthog_events_ingestion` — pair with PostHog for error-vs-behavior correlation
- `dataframe_to_snowflake` / `dataframe_to_bigquery` — persist issues to your warehouse for long-term trends
