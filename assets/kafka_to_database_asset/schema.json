{
  "type": "dagster_component_templates.kafka_to_database_asset",
  "name": "Kafka to Database",
  "category": "assets",
  "description": "Consume a batch of messages from a Kafka topic and write them to a database table",
  "dependencies": {
    "pip": [
      "confluent-kafka>=2.0.0",
      "pandas>=1.3.0",
      "sqlalchemy>=2.0.0"
    ]
  },
  "tags": [
    "assets",
    "kafka",
    "streaming",
    "ingestion",
    "sql"
  ]
}