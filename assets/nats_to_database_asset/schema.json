{
  "type": "dagster_component_templates.nats_to_database_asset",
  "name": "NATS to Database",
  "category": "assets",
  "description": "Fetch messages from a NATS subject or JetStream consumer and write them to a database table",
  "dependencies": {
    "pip": [
      "nats-py>=2.6.0",
      "pandas>=1.3.0",
      "sqlalchemy>=2.0.0"
    ]
  },
  "tags": [
    "assets",
    "nats",
    "jetstream",
    "ingestion",
    "sql"
  ]
}
