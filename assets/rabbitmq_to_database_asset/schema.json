{
  "type": "dagster_component_templates.rabbitmq_to_database_asset",
  "name": "RabbitMQ to Database",
  "category": "assets",
  "description": "Drain messages from a RabbitMQ queue and write them to a database table",
  "dependencies": {
    "pip": [
      "pika>=1.3.0",
      "pandas>=1.3.0",
      "sqlalchemy>=2.0.0"
    ]
  },
  "tags": [
    "assets",
    "rabbitmq",
    "amqp",
    "ingestion",
    "sql"
  ]
}
