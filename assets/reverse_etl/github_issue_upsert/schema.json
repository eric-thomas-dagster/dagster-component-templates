{
  "component_type": "dagster_component_templates.GitHubIssueUpsertComponent",
  "description": "Reverse-ETL sink: mirror an upstream DataFrame into GitHub Issues in a target repo. Rows are matched to existing issues by a stable key marker embedded in the issue body, not by title (so a human editing the title doesn't break the sync).",
  "category": "reverse_etl",
  "icon": "si:github",
  "x-dagster-io": {
    "inputs": {"type": "dataframe", "description": "pandas DataFrame from an upstream asset. Each row -> one GitHub issue (upserted via key_column)."},
    "outputs": {"type": "sink", "description": "Dagster asset materialization with metadata (rows_created / rows_updated / rows_closed). No downstream data -- write side effect lands in GitHub."}
  },
  "attributes": {
    "asset_name": {"type": "string", "label": "Asset Name", "required": true},
    "upstream_asset_key": {"type": "string", "label": "Upstream Asset Key", "required": false, "description": "Asset providing the DataFrame. Mutually exclusive with source."},
    "source": {"type": "object", "label": "Source", "required": false, "description": "Inline source config (sql/csv/inline). Mutually exclusive with upstream_asset_key."},
    "repo": {"type": "string", "label": "Repo", "required": true, "description": "Target repo in 'owner/name' form."},
    "resource_key": {"type": "string", "label": "Resource Key", "required": false, "default": "github"},
    "key_column": {"type": "string", "label": "Key Column", "required": true, "description": "Upstream column holding a stable unique key. Written into each issue's body as a marker and used to match rows to existing issues on subsequent runs."},
    "title_column": {"type": "string", "label": "Title Column", "required": true, "description": "Column holding the issue title."},
    "body_column": {"type": "string", "label": "Body Column", "required": false, "default": null, "description": "Column holding the issue body (markdown). Leave empty for title-only issues."},
    "labels_column": {"type": "string", "label": "Labels Column", "required": false, "default": null, "description": "Column holding labels. Accepts a list, or a comma-separated string."},
    "state_column": {"type": "string", "label": "State Column", "required": false, "default": null, "description": "Column holding state ('open' or 'closed'). Defaults to 'open' if unset."},
    "assignees_column": {"type": "string", "label": "Assignees Column", "required": false, "default": null, "description": "Column holding assignee logins. Accepts a list, or a comma-separated string."},
    "default_labels": {"type": "array", "items": {"type": "string"}, "label": "Default Labels", "required": false, "default": [], "description": "Labels always applied to every synced issue, on top of labels_column.", "ui:widget": "list"},
    "close_missing": {"type": "boolean", "label": "Close Missing", "required": false, "default": false, "description": "If true, close open issues whose key marker is NOT in the upstream DataFrame.", "ui:widget": "checkbox"},
    "batch_size": {"type": "integer", "label": "Batch Size", "required": false, "default": 100, "description": "Max upstream rows to process per run (safety cap)."},
    "group_name": {"type": "string", "label": "Group Name", "required": false, "default": "github"},
    "description": {"type": "string", "label": "Description", "required": false},
    "owners": {"type": "array", "items": {"type": "string"}, "label": "Owners", "required": false, "ui:widget": "list"},
    "tags": {"type": "object", "label": "Tags", "required": false, "ui:widget": "key-value"},
    "kinds": {"type": "array", "items": {"type": "string"}, "label": "Kinds", "required": false, "ui:widget": "list"}
  },
  "tags": ["github", "reverse-etl", "upsert", "devops"],
  "name": "GitHub Issue Upsert"
}
