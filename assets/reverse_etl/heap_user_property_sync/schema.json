{
  "component_type": "dagster_component_templates.HeapUserPropertySyncComponent",
  "description": "Reverse-ETL sink: push computed user properties from an upstream DataFrame into Heap via add_user_properties. Heap's API is write-only with no read-back or dedup concept -- this calls the endpoint once per row every run.",
  "category": "reverse_etl",
  "icon": "si:heap",
  "x-dagster-io": {
    "inputs": {"type": "dataframe", "description": "pandas DataFrame from an upstream asset. Each row pushes properties onto one Heap user (matched via identity_column)."},
    "outputs": {"type": "sink", "description": "Dagster asset materialization with metadata (rows_synced / rows_errored). No downstream data -- write side effect lands in Heap."}
  },
  "attributes": {
    "asset_name": {"type": "string", "label": "Asset Name", "required": true},
    "upstream_asset_key": {"type": "string", "label": "Upstream Asset Key", "required": false, "description": "Asset providing the DataFrame. Mutually exclusive with source."},
    "source": {"type": "object", "label": "Source", "required": false, "description": "Inline source config (sql/csv/inline). Mutually exclusive with upstream_asset_key."},
    "resource_key": {"type": "string", "label": "Resource Key", "required": false, "default": "heap"},
    "identity_column": {"type": "string", "label": "Identity Column", "required": true, "description": "Upstream column holding the Heap identity (typically an email or internal user ID)."},
    "properties_map": {"type": "object", "label": "Properties Map", "required": true, "description": "Upstream column -> Heap user property name.", "ui:widget": "key-value"},
    "max_rows": {"type": "integer", "label": "Max Rows", "required": false, "default": 10000, "description": "Overall safety cap on rows per run."},
    "group_name": {"type": "string", "label": "Group Name", "required": false, "default": "heap"},
    "description": {"type": "string", "label": "Description", "required": false},
    "owners": {"type": "array", "items": {"type": "string"}, "label": "Owners", "required": false, "ui:widget": "list"},
    "tags": {"type": "object", "label": "Tags", "required": false, "ui:widget": "key-value"},
    "kinds": {"type": "array", "items": {"type": "string"}, "label": "Kinds", "required": false, "ui:widget": "list"}
  },
  "tags": ["heap", "reverse-etl", "sync", "marketing"],
  "name": "Heap User Property Sync"
}
