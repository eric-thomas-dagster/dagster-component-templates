{
  "type": "dagster_component_templates.CosmosdbWriterComponent",
  "name": "CosmosDB Writer",
  "category": "sink",
  "description": "Write a DataFrame to an Azure Cosmos DB container with upsert or insert mode",
  "icon": "Database",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "description": "DataFrame to write to Cosmos DB"
    },
    "outputs": {
      "type": "materialize_result",
      "description": "Materialization result with row count metadata"
    },
    "supports_partitions": false
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "required": true,
      "label": "Asset Name",
      "description": "Name of the output asset to create",
      "placeholder": "write_to_cosmosdb"
    },
    "upstream_asset_key": {
      "type": "string",
      "required": true,
      "label": "Upstream Asset Key",
      "description": "Asset key of the upstream DataFrame asset"
    },
    "endpoint_env_var": {
      "type": "string",
      "required": false,
      "label": "Endpoint Env Var",
      "description": "Environment variable containing the Cosmos DB endpoint URL",
      "default": "COSMOS_ENDPOINT"
    },
    "key_env_var": {
      "type": "string",
      "required": false,
      "label": "Key Env Var",
      "description": "Environment variable containing the Cosmos DB primary key",
      "default": "COSMOS_KEY"
    },
    "database": {
      "type": "string",
      "required": true,
      "label": "Database",
      "description": "Cosmos DB database name"
    },
    "container": {
      "type": "string",
      "required": true,
      "label": "Container",
      "description": "Cosmos DB container name"
    },
    "if_exists": {
      "type": "string",
      "required": false,
      "label": "If Exists",
      "description": "Write mode: 'upsert' (create or replace) or 'insert' (create only)",
      "default": "upsert",
      "enum": [
        "upsert",
        "insert"
      ]
    },
    "group_name": {
      "type": "string",
      "required": false,
      "label": "Group Name",
      "description": "Asset group for organization"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "dependencies": {
    "pip": [
      "pandas>=1.5.0",
      "azure-cosmos>=4.0.0"
    ]
  },
  "tags": [
    "sink",
    "cosmosdb",
    "azure",
    "nosql",
    "document-database"
  ]
}
