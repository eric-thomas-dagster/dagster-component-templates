{
  "component_type": "dagster_component_templates.DataframeToAdlsComponent",
  "description": "Write a DataFrame to Azure Data Lake Storage Gen2.",
  "category": "sink",
  "icon": "ArrowDownToLine",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "DataFrame to write"
    },
    "outputs": {
      "type": "MaterializeResult",
      "description": "Write confirmation with metadata"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Upstream asset key providing a DataFrame",
      "required": true
    },
    "account_name_env_var": {
      "type": "string",
      "label": "Account Name Env Var",
      "description": "Env var containing the Azure storage account name",
      "required": false,
      "default": "AZURE_STORAGE_ACCOUNT"
    },
    "container": {
      "type": "string",
      "label": "Container",
      "description": "ADLS container / filesystem name",
      "required": true
    },
    "blob_path": {
      "type": "string",
      "label": "Blob Path",
      "description": "Path within container e.g. processed/results.parquet",
      "required": true
    },
    "format": {
      "type": "string",
      "label": "Format",
      "description": "Output format: ",
      "required": false,
      "default": "parquet"
    },
    "account_key_env_var": {
      "type": "string",
      "label": "Account Key Env Var",
      "description": "Account Key Env Var",
      "required": false,
      "default": null
    },
    "connection_string_env_var": {
      "type": "string",
      "label": "Connection String Env Var",
      "description": "Connection String Env Var",
      "required": false,
      "default": null
    },
    "compression": {
      "type": "string",
      "label": "Compression",
      "description": "Compression codec. For parquet: ",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "sink",
    "dataframe",
    "adls"
  ]
}
