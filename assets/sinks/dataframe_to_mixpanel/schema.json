{
  "component_type": "dagster_component_templates.DataframeToMixpanelComponent",
  "description": "Reverse-ETL sink — POST DataFrame rows to Mixpanel via bulk endpoints. Two modes: `events` (POST /import, service-account auth, up to 2000/batch) or `profiles` (POST /engage#profile-set, project-token auth, up to 50/batch).",
  "category": "sink",
  "icon": "si:mixpanel",
  "attributes": {
    "asset_name": {"type": "string", "required": true},
    "upstream_asset_key": {"type": "string", "required": false},
    "source": {"type": "object", "required": false},
    "resource_key": {"type": "string", "required": true},
    "mode": {"type": "string", "enum": ["events", "profiles"], "default": "events", "required": false},
    "event_column": {"type": "string", "required": false, "description": "Required for mode='events' (or set `default_event`)."},
    "default_event": {"type": "string", "required": false},
    "distinct_id_column": {"type": "string", "required": true},
    "time_column": {"type": "string", "required": false, "description": "For events mode. Coerced to unix seconds."},
    "insert_id_column": {"type": "string", "required": false, "description": "For events mode. Dedup key ($insert_id)."},
    "properties_map": {"type": "object", "required": false},
    "property_columns": {"type": "array", "items": {"type": "string"}, "required": false},
    "batch_size": {"type": "integer", "required": false, "description": "Default 2000 for events, 50 for profiles (Mixpanel maxes)."},
    "dry_run": {"type": "boolean", "required": false, "default": false},
    "group_name": {"type": "string", "required": false},
    "description": {"type": "string", "required": false},
    "deps": {"type": "array", "items": {"type": "string"}, "required": false},
    "owners": {"type": "array", "items": {"type": "string"}, "required": false},
    "asset_tags": {"type": "object", "required": false},
    "kinds": {"type": "array", "items": {"type": "string"}, "required": false},
    "retry_policy_max_retries": {"type": "integer", "required": false},
    "retry_policy_delay_seconds": {"type": "integer", "required": false},
    "retry_policy_backoff": {"type": "string", "required": false, "default": "exponential"},
    "freshness_max_lag_minutes": {"type": "integer", "required": false},
    "freshness_cron": {"type": "string", "required": false},
    "partition_type": {"type": "string", "required": false},
    "partition_start": {"type": "string", "required": false},
    "partition_values": {"type": "string", "required": false},
    "dynamic_partition_name": {"type": "string", "required": false}
  },
  "tags": ["sink", "mixpanel", "reverse-etl", "product-analytics", "event-tracking", "user-profiles"],
  "name": "DataFrame to Mixpanel"
}
