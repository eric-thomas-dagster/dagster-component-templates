{
  "component_type": "dagster_component_templates.DataframeToOtlpMetricsComponent",
  "description": "Push DataFrame rows as OpenTelemetry metrics via OTLP/HTTP. ONE sink, ANY backend (OTel collector, Honeycomb, Lightstep, Datadog, Splunk Observability, Grafana Cloud, etc.).",
  "category": "sink",
  "icon": "LineChart",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "description": "Upstream DataFrame"
    },
    "outputs": {
      "type": "MaterializeResult",
      "description": "Write confirmation with metadata"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "required": true,
      "description": "Dagster asset name"
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "required": true,
      "description": "Asset key of upstream DataFrame"
    },
    "endpoint": {
      "type": "string",
      "label": "Endpoint",
      "required": true,
      "description": "OTLP/HTTP endpoint base URL, e.g. 'http://localhost:4318' (OTel collector default), 'https://api.honeycomb.io', 'https://otlp.eu01.nr-data.net'."
    },
    "metric_name": {
      "type": "string",
      "label": "Metric Name",
      "required": true,
      "description": "OTel metric name (dot-namespaced, e.g. 'orders.processed.total')"
    },
    "metric_kind": {
      "type": "string",
      "label": "Metric Kind",
      "required": false,
      "description": "'gauge' | 'sum' (counter/up-down). Histograms are advanced — use a custom op for those.",
      "default": "gauge"
    },
    "metric_unit": {
      "type": "string",
      "label": "Metric Unit",
      "required": false,
      "description": "UCUM unit, e.g. '1', 'ms', 'By'",
      "default": null
    },
    "value_column": {
      "type": "string",
      "label": "Value Column",
      "required": true,
      "description": "DataFrame column with numeric metric values"
    },
    "attribute_columns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "ui:widget": "list",
      "label": "Attribute Columns",
      "required": false,
      "description": "Columns whose values become OTel attributes (labels) on each metric data point",
      "default": null
    },
    "timestamp_column": {
      "type": "string",
      "label": "Timestamp Column",
      "required": false,
      "description": "Column with epoch-seconds (or pandas datetime). Default: now() per row.",
      "default": null
    },
    "service_name": {
      "type": "string",
      "label": "Service Name",
      "required": false,
      "description": "OTel resource service.name attribute — identifies the producer",
      "default": "dagster"
    },
    "bearer_token_env_var": {
      "type": "string",
      "label": "Bearer Token Env Var",
      "required": false,
      "description": "Env var with Bearer token for Authorization header (most SaaS backends require this)",
      "default": null
    },
    "extra_headers": {
      "type": "object",
      "label": "Extra Headers",
      "required": false,
      "description": "Extra HTTP headers, e.g. {x-honeycomb-dataset: production} or {api-key: ...}",
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": false,
      "default": null,
      "description": "Dagster asset group name"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "required": false
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "required": false
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "required": false,
      "description": "Asset owners — team names (team:analytics) or email addresses.",
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "required": false,
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "required": false,
      "description": "Asset kinds shown in the Dagster catalog. Auto-inferred from component name if empty.",
      "ui:widget": "list"
    },
    "retry_policy_max_retries": {
      "type": "integer",
      "label": "Retry Policy Max Retries",
      "required": false
    },
    "retry_policy_delay_seconds": {
      "type": "integer",
      "label": "Retry Policy Delay Seconds",
      "required": false
    },
    "retry_policy_backoff": {
      "type": "string",
      "label": "Retry Policy Backoff",
      "required": false,
      "default": "exponential",
      "enum": [
        "exponential",
        "linear",
        "fixed"
      ],
      "ui:widget": "select"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "required": false,
      "default": null,
      "description": "Maximum acceptable lag in minutes before this asset is considered stale."
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "required": false,
      "default": null,
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "x-dagster-widget": "cron"
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "required": false,
      "default": null,
      "description": "Enable partitioning: 'daily'/'weekly'/'monthly'/'hourly' for time-based; 'static' for a fixed set of values; 'dynamic' for values added at runtime; 'multi' for date × static. Leave empty for unpartitioned.",
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "dynamic",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "required": false,
      "default": null,
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based partition types."
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "required": false,
      "default": null,
      "description": "Comma-separated values for static or multi partitioning."
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "required": false,
      "default": null,
      "description": "Name for DynamicPartitionsDefinition when partition_type='dynamic'."
    }
  }
}
