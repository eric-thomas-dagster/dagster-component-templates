{
  "component_type": "dagster_component_templates.FreshdeskContactUpsertComponent",
  "description": "Reverse-ETL sink: mirror an upstream DataFrame into Freshdesk contacts via search-then-write. Freshdesk has no single-call native upsert, so this lists /contacts filtered by unique_external_id, then updates the match or creates a new contact.",
  "category": "sink",
  "icon": "si:freshworks",
  "x-dagster-io": {
    "inputs": {"type": "dataframe", "description": "pandas DataFrame from an upstream asset. Each row -> one Freshdesk contact (upserted via unique_external_id)."},
    "outputs": {"type": "sink", "description": "Dagster asset materialization with metadata (rows_upserted / rows_errored). No downstream data -- write side effect lands in Freshdesk."}
  },
  "attributes": {
    "asset_name": {"type": "string", "label": "Asset Name", "required": true},
    "upstream_asset_key": {"type": "string", "label": "Upstream Asset Key", "required": false, "description": "Asset providing the DataFrame. Mutually exclusive with source."},
    "source": {"type": "object", "label": "Source", "required": false, "description": "Inline source config (sql/csv/inline). Mutually exclusive with upstream_asset_key."},
    "resource_key": {"type": "string", "label": "Resource Key", "required": false, "default": "freshdesk_resource"},
    "external_id_column": {"type": "string", "label": "External Id Column", "required": true, "description": "Upstream column holding a stable external ID (Freshdesk match key via unique_external_id)."},
    "fields_map": {"type": "object", "label": "Fields Map", "required": true, "description": "Upstream column -> Freshdesk contact field (e.g. name, email, phone).", "ui:widget": "key-value"},
    "max_rows": {"type": "integer", "label": "Max Rows", "required": false, "default": 10000, "description": "Safety cap on total rows per run."},
    "group_name": {"type": "string", "label": "Group Name", "required": false, "default": "freshdesk"},
    "description": {"type": "string", "label": "Description", "required": false},
    "owners": {"type": "array", "items": {"type": "string"}, "label": "Owners", "required": false, "ui:widget": "list"},
    "tags": {"type": "object", "label": "Tags", "required": false, "ui:widget": "key-value"},
    "kinds": {"type": "array", "items": {"type": "string"}, "label": "Kinds", "required": false, "ui:widget": "list"}
  },
  "tags": ["freshdesk", "sink", "reverse-etl", "upsert", "support"],
  "name": "Freshdesk Contact Upsert"
}
