{
  "component_type": "dagster_component_templates.DataframeJoin",
  "description": "Join two DataFrame assets on common or specified columns.",
  "category": "transformation",
  "icon": "Shuffle",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Transformed DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Output Dagster asset name",
      "required": true
    },
    "left_asset_key": {
      "type": "string",
      "label": "Left Asset Key",
      "description": "Left DataFrame asset key",
      "required": true
    },
    "right_asset_key": {
      "type": "string",
      "label": "Right Asset Key",
      "description": "Right DataFrame asset key",
      "required": true
    },
    "how": {
      "type": "string",
      "label": "How",
      "description": "Join type: ",
      "required": false,
      "default": "inner"
    },
    "on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "On",
      "description": "On",
      "required": false,
      "default": null,
      "ui:widget": "list"
    },
    "left_on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Left On",
      "description": "Left On",
      "required": false,
      "default": null,
      "ui:widget": "list"
    },
    "right_on": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Right On",
      "description": "Right On",
      "required": false,
      "default": null,
      "ui:widget": "list"
    },
    "suffixes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Suffixes",
      "description": "Suffixes for overlapping column names",
      "required": false,
      "default": "[\"_x\"",
      "ui:widget": "list"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Dagster asset group name",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "transformation",
    "dataframe",
    "join"
  ]
}
