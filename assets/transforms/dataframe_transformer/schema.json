{
  "component_type": "dagster_component_templates.DataFrameTransformerComponent",
  "description": "Component for transforming DataFrames from upstream assets.",
  "category": "transformation",
  "icon": "Shuffle",
  "x-dagster-io": {
    "inputs": {
      "type": "dataframe",
      "required": true,
      "description": "Input DataFrame"
    },
    "outputs": {
      "type": "dataframe",
      "description": "Transformed DataFrame"
    }
  },
  "attributes": {
    "asset_name": {
      "type": "string",
      "label": "Asset Name",
      "description": "Name of this asset",
      "required": true
    },
    "filter_columns": {
      "type": "string",
      "label": "Filter Columns",
      "description": "Comma-separated list of columns to keep",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "drop_columns": {
      "type": "string",
      "label": "Drop Columns",
      "description": "Comma-separated list of columns to drop",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "rename_columns": {
      "type": "string",
      "label": "Rename Columns",
      "description": "JSON mapping of column renames: ",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "drop_duplicates": {
      "type": "boolean",
      "label": "Drop Duplicates",
      "description": "Whether to drop duplicate rows",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "drop_na": {
      "type": "boolean",
      "label": "Drop Na",
      "description": "Whether to drop rows with NA values",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "fill_na_value": {
      "type": "string",
      "label": "Fill Na Value",
      "description": "Value to fill NA values with",
      "required": false,
      "default": null
    },
    "filter_expression": {
      "type": "string",
      "label": "Filter Expression",
      "description": "Pandas query expression (e.g., ",
      "required": false,
      "default": null,
      "ui:widget": "code"
    },
    "sort_by": {
      "type": "string",
      "label": "Sort By",
      "description": "Comma-separated columns to sort by",
      "required": false,
      "default": null,
      "x-dagster-widget": "columns"
    },
    "sort_ascending": {
      "type": "boolean",
      "label": "Sort Ascending",
      "description": "Sort direction",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "group_by": {
      "type": "string",
      "label": "Group By",
      "description": "Comma-separated columns to group by",
      "required": false,
      "default": null,
      "x-dagster-widget": "columns"
    },
    "agg_functions": {
      "type": "string",
      "label": "Agg Functions",
      "description": "JSON mapping of aggregations: ",
      "required": false,
      "default": null
    },
    "combine_method": {
      "type": "string",
      "label": "Combine Method",
      "description": "How to combine multiple DataFrames: ",
      "required": false,
      "default": "concat"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Asset description",
      "required": false,
      "default": null,
      "ui:widget": "code"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group for organization",
      "required": false,
      "default": null
    },
    "partition_type": {
      "type": "string",
      "label": "Partition Type",
      "description": "Enable partitioning: 'daily', 'weekly', 'monthly', 'hourly' for time-based; 'static' for a fixed set of values (e.g. customers); 'multi' for date \u00d7 static (e.g. day \u00d7 customer). Leave empty for unpartitioned.",
      "required": false,
      "default": null,
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "hourly",
        "static",
        "multi"
      ],
      "ui:widget": "select"
    },
    "partition_start": {
      "type": "string",
      "label": "Partition Start Date",
      "description": "ISO date string for the first partition, e.g. '2024-01-01'. Required for time-based and multi partition types.",
      "required": false,
      "default": null
    },
    "partition_date_column": {
      "type": "string",
      "label": "Partition Date Column",
      "description": "Column used to filter rows to the current date partition key (e.g. 'event_date', 'created_at').",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "partition_values": {
      "type": "string",
      "label": "Partition Values",
      "description": "Comma-separated values for static or multi partitioning, e.g. 'acme_corp,globex,initech'. Required for static and multi types.",
      "required": false,
      "default": null
    },
    "partition_static_dim": {
      "type": "string",
      "label": "Static Dimension Name",
      "description": "Name for the static partition dimension in multi-partitioning, e.g. 'customer' or 'region'. Defaults to 'segment'.",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "partition_static_column": {
      "type": "string",
      "label": "Static Partition Column",
      "description": "Column used to filter rows to the current static partition value (e.g. 'customer_id', 'region').",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "description": "Asset owners \u2014 team names (team:analytics) or email addresses.",
      "required": false,
      "ui:widget": "list"
    },
    "asset_tags": {
      "type": "object",
      "label": "Asset Tags",
      "description": "Key-value tags applied to the asset in the Dagster catalog, e.g. {\"domain\": \"finance\"}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "description": "Asset kinds shown in the Dagster catalog (e.g. snowflake, python). Auto-inferred from component name if empty.",
      "required": false,
      "ui:widget": "list"
    },
    "freshness_max_lag_minutes": {
      "type": "integer",
      "label": "Freshness Max Lag (minutes)",
      "description": "Maximum acceptable lag in minutes before this asset is considered stale.",
      "required": false,
      "default": null
    },
    "freshness_cron": {
      "type": "string",
      "label": "Freshness Cron Schedule",
      "description": "Cron schedule for the freshness policy, e.g. '0 9 * * 1-5' (weekdays 9am).",
      "required": false,
      "default": null,
      "x-dagster-widget": "cron"
    },
    "column_lineage": {
      "type": "object",
      "label": "Column Lineage",
      "description": "Column-level lineage: output column \u2192 list of upstream input columns it derives from, e.g. {\"revenue\": [\"price\", \"quantity\"]}.",
      "required": false,
      "ui:widget": "key-value"
    },
    "string_operations": {
      "type": "string",
      "label": "String Operations",
      "description": "JSON list of string operations: [{",
      "required": false,
      "default": null
    },
    "string_replace": {
      "type": "string",
      "label": "String Replace",
      "description": "JSON mapping of string replacements: {",
      "required": false,
      "default": null
    },
    "calculated_columns": {
      "type": "string",
      "label": "Calculated Columns",
      "description": "JSON mapping of calculated columns: {",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "pivot_config": {
      "type": "string",
      "label": "Pivot Config",
      "description": "JSON config for pivot: {",
      "required": false,
      "default": null
    },
    "unpivot_config": {
      "type": "string",
      "label": "Unpivot Config",
      "description": "JSON config for unpivot/melt: {",
      "required": false,
      "default": null
    },
    "upstream_asset_key": {
      "type": "string",
      "label": "Upstream Asset Key",
      "description": "Single upstream asset key. Convenience field for the common single-source case.",
      "required": false,
      "default": null
    },
    "upstream_asset_keys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Upstream Asset Keys",
      "description": "Multiple upstream asset keys for multi-source transforms.",
      "required": false,
      "default": null
    },
    "include_preview_metadata": {
      "type": "boolean",
      "label": "Include Preview Metadata",
      "description": "Include Preview Metadata",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "preview_rows": {
      "type": "integer",
      "label": "Preview Rows",
      "description": "How many rows to include in the preview. Large tables (>10\u00d7 this number) are sampled randomly.",
      "required": false,
      "default": 25
    },
    "dynamic_partition_name": {
      "type": "string",
      "label": "Dynamic Partition Name",
      "description": "Name for DynamicPartitionsDefinition (when partition_type='dynamic'), e.g. 'tenants'.",
      "required": false,
      "default": null
    },
    "partition_dimensions": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "label": "Partition Dimensions",
      "description": "Multi-axis partition spec: list of {name, type, start, values, dynamic_partition_name} dicts. Overrides flat fields when set.",
      "required": false,
      "default": null
    }
  },
  "tags": [
    "transformation",
    "dataframe",
    "transformer"
  ],
  "name": "DataFrame Transformer"
}
