{
  "additionalProperties": false,
  "description": "Generic DynamicOut fan-out AS AN ASSET: discover \u2192 map \u2192 collect.",
  "properties": {
    "asset_name": {
      "description": "Name of the emitted @graph_asset.",
      "title": "Asset Name",
      "type": "string"
    },
    "group_name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Asset group.",
      "title": "Group Name"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Asset description.",
      "title": "Description"
    },
    "owners": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Asset owners.",
      "title": "Owners"
    },
    "tags": {
      "anyOf": [
        {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Extra asset tags.",
      "title": "Tags"
    },
    "kinds": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Asset kinds. Defaults to ['fanout', 'transform'].",
      "title": "Kinds"
    },
    "upstream_asset_key": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional Dagster asset key to load and pass into the discover callable as `upstream=<value>`. When None, discover fetches its items from its own source (URL list, filesystem, queue, etc.).",
      "title": "Upstream Asset Key"
    },
    "deps": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional list of upstream Dagster asset keys as lineage-only dependencies \u2014 declares graph edges without loading data through the IO manager. Use for assets whose materialization doesn't write a value to the IO manager (e.g. dbt models writing to an external warehouse). For value-loading upstreams use `upstream_asset_key` instead.",
      "title": "Deps"
    },
    "discover_callable_path": {
      "description": "'module:function' that returns an iterable of items. Signature: discover(upstream=<loaded>, **discover_kwargs) \u2192 List[item]. The upstream kwarg is only passed when upstream_asset_key is set.",
      "title": "Discover Callable Path",
      "type": "string"
    },
    "discover_kwargs": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Static kwargs passed to discover.",
      "title": "Discover Kwargs"
    },
    "process_callable_path": {
      "description": "'module:function' that takes one item and returns a result. Signature: process(item, **process_kwargs).",
      "title": "Process Callable Path",
      "type": "string"
    },
    "process_kwargs": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Static kwargs merged into each process call.",
      "title": "Process Kwargs"
    },
    "collect_callable_path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional 'module:function' that takes the list of process results and returns the asset's final value. If omitted, the asset materializes as the raw list of process results.",
      "title": "Collect Callable Path"
    },
    "mapping_key_field": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "If items are dicts, use this field as the DynamicOutput mapping_key so per-item retries are stable across runs. None \u2192 the loop index is used (stable within a run, not across).",
      "title": "Mapping Key Field"
    },
    "max_concurrent_tag_value": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "If set, applies `dagster/concurrency_key=<value>` to the process op so an instance-level concurrency limit governs fan-out parallelism.",
      "title": "Max Concurrent Tag Value"
    },
    "retry_max_retries": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Per-item retry max.",
      "title": "Retry Max Retries"
    },
    "retry_delay_seconds": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Retry Delay Seconds"
    },
    "retry_backoff": {
      "default": "exponential",
      "title": "Retry Backoff",
      "type": "string"
    },
    "fail_on_empty": {
      "default": false,
      "description": "Fail the graph if discover returns no items.",
      "title": "Fail On Empty",
      "type": "boolean"
    }
  },
  "required": [
    "asset_name",
    "discover_callable_path",
    "process_callable_path"
  ],
  "title": "DynamicFanoutAssetComponent",
  "type": "object"
}