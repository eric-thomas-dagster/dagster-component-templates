{
  "component_type": "dagster_component_templates.WarehouseMultiFieldFormulaComponent",
  "description": "Apply one formula template to N columns via CTAS. Alteryx Multi-Field Formula equivalent.",
  "category": "transformation",
  "icon": "Calculator",
  "x-dagster-io": {
    "inputs": {
      "type": "table",
      "required": true
    },
    "outputs": {
      "type": "table"
    }
  },
  "attributes": {
    "asset_name": {"label": "Asset Name", 
      "type": "string",
      "required": true
    },
    "database_url": {"label": "Database URL", 
      "type": "string"
    },
    "database_url_env_var": {"label": "Database URL Env Var", 
      "type": "string"
    },
    "dialect": {"label": "Dialect", 
      "type": "string",
      "required": true,
      "enum": [
        "duckdb",
        "postgres",
        "postgresql",
        "snowflake",
        "bigquery",
        "redshift",
        "databricks",
        "mssql",
        "mysql"
      ]
    },
    "upstream_table": {"label": "Upstream Table", 
      "type": "string",
      "required": true
    },
    "output_table": {"label": "Output Table", 
      "type": "string",
      "required": true
    },
    "expression": {"label": "Expression", 
      "type": "string",
      "required": true,
      "description": "Formula template with {col} as the column-name placeholder. e.g. 'UPPER(TRIM({col}))'."
    },
    "columns": {"label": "Columns", 
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": true,
      "x-dagster-widget": "columns"
    },
    "output_mode": {"label": "Output Mode", 
      "type": "string",
      "enum": [
        "replace",
        "add_suffix",
        "add_prefix"
      ],
      "default": "add_suffix"
    },
    "suffix": {"label": "Suffix", 
      "type": "string",
      "default": "_calc"
    },
    "prefix": {"label": "Prefix", 
      "type": "string",
      "default": "calc_"
    },
    "mode": {"label": "Mode", 
      "type": "string",
      "enum": [
        "replace",
        "create_if_not_exists"
      ],
      "default": "replace"
    },
    "group_name": {"label": "Group Name", 
      "type": "string"
    },
    "deps": {"label": "Deps", 
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "owners": {"label": "Owners", 
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "description": {"label": "Description", 
      "type": "string"
    },
    "asset_tags": {"label": "Asset Tags", 
      "type": "object"
    },
    "kinds": {"label": "Kinds", 
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "include_preview_metadata": {"label": "Include Preview Metadata", 
      "type": "boolean",
      "default": false
    },
    "preview_rows": {"label": "Preview Rows", 
      "type": "integer",
      "default": 25,
      "minimum": 1,
      "maximum": 200
    }
  }
}
