{
  "type": "dagster_component_templates.warehouse_schema_assets",
  "name": "Warehouse Schema Assets",
  "category": "assets",
  "description": "Auto-discovers tables in database schemas and creates one external Dagster asset per table, with column schemas and cached introspection via StateBackedComponent",
  "dependencies": {
    "pip": ["sqlalchemy>=2.0.0"]
  },
  "tags": ["assets", "warehouse", "schema", "external", "lineage", "state-backed", "sql"]
}
