# external_cube_metric

Declare a Cube semantic-layer metric (measure OR dimension) as an external Dagster asset. Cube owns the definition; Dagster puts it in the catalog with lineage.

## Example

```yaml
type: dagster_community_components.ExternalCubeMetricAsset
attributes:
  asset_key: cube/orders/count
  cube_name: Orders
  measure_name: Orders.count
  metric_type: count
  cube_playground_url: http://localhost:4000/#/build
```

For dimensions:

```yaml
attributes:
  asset_key: cube/orders/status
  cube_name: Orders
  dimension_name: Orders.status
  metric_type: string
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_key` | `str` | Dagster asset key, '/'-separated (e.g. 'cube/orders/count'). |
| `cube_name` | `str` | Cube name (e.g. 'Orders', 'Customers'). |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `group_name` | `str` | `"cube"` | Dagster asset group name. |
| `description` | `str` | — | Asset description. |
| `owners` | `List[str]` | — | Asset owners. |
| `tags` | `Dict[str, str]` | — | Catalog tags. |
| `kinds` | `List[str]` | — | Asset kinds (auto-includes 'cube', 'semantic-layer'). |
| `deps` | `List[str]` | — | Upstream Dagster asset keys. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `measure_name` | `str` | — | Cube measure name (e.g. 'Orders.count', 'Orders.totalAmount'). Set this OR dimension_name. |
| `dimension_name` | `str` | — | Cube dimension name (e.g. 'Orders.status'). Set this OR measure_name. |
| `metric_type` | `str` | — | Cube type (count / sum / avg / string / time / number / boolean). Surfaced as metadata. |
| `cube_playground_url` | `str` | — | Cube Playground URL — clickable link in the catalog to inspect the metric interactively. |
| `cube_docs_url` | `str` | — | URL to the metric's docs / schema file (GitHub / internal wiki). |

[//]: # (FIELDS:END)

## Related

- [`cube_query_asset`](../../assets/analytics/cube_query_asset) — execute Cube queries and materialize DataFrames.
