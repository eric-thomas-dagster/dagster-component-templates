{
  "component_type": "dagster_component_templates.ExternalDorisTableAsset",
  "description": "Declare an Apache Doris table as an external Dagster asset.\n\nExample:\n    ```yaml\n    type: dagster_community_components.ExternalDorisTableAsset\n    attributes:\n      asset_key: doris/analytics/events_realtime\n      database: analytics\n      table: events_realtime\n      fe_host: doris-fe.mycompany.com\n      group_name: doris\n      description: |\n        Real-time events table populated by Routine Load from Kafka.\n    ```",
  "category": "external",
  "icon": "si:apache",
  "attributes": {
    "asset_key": {
      "type": "string",
      "label": "Asset Key",
      "required": true,
      "description": "Dagster asset key (e.g. 'doris/analytics/events_realtime')."
    },
    "database": {
      "type": "string",
      "label": "Database",
      "required": true,
      "description": "Doris database (catalog) name."
    },
    "table": {
      "type": "string",
      "label": "Table",
      "required": true,
      "description": "Doris table name."
    },
    "fe_host": {
      "type": "string",
      "label": "Fe Host",
      "required": false,
      "description": "Doris Frontend host — surfaced as catalog metadata.",
      "default": null
    },
    "table_model": {
      "type": "string",
      "label": "Table Model",
      "required": false,
      "description": "Doris table model: 'duplicate' (default) / 'unique' / 'aggregate' / 'mv' (materialized view). Surfaced as metadata; doesn't affect the declare-only AssetSpec.",
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": false,
      "default": null,
      "description": "Dagster asset group name"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "required": false
    },
    "owners": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Owners",
      "required": false,
      "description": "Asset owners — team names (team:analytics) or email addresses.",
      "ui:widget": "list"
    },
    "tags": {
      "type": "object",
      "label": "Tags",
      "required": false,
      "description": "Catalog tags.",
      "default": null
    },
    "kinds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Kinds",
      "required": false,
      "description": "Asset kinds shown in the Dagster catalog. Auto-inferred from component name if empty.",
      "ui:widget": "list"
    },
    "deps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "label": "Deps",
      "required": false
    }
  }
}
