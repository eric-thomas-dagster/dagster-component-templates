# external_starrocks_table

## Purpose

Declare-only AssetSpec for a StarRocks table that StarRocks itself owns the lifecycle of (Routine Load from Kafka, Flink CDC, etc.). Mirror of external_doris_table.

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_key` | `str` | Dagster asset key. |
| `database` | `str` | StarRocks database (catalog) name. |
| `table` | `str` | StarRocks table name. |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `group_name` | `str` | `"starrocks"` | Dagster asset group name. |
| `description` | `str` | — | — |
| `owners` | `List[str]` | — | — |
| `tags` | `Dict[str, str]` | — | — |
| `kinds` | `List[str]` | — | — |
| `deps` | `List[str]` | — | — |

### Source / target

| Field | Type | Default | Description |
|---|---|---|---|
| `table_model` | `str` | — | StarRocks table model: 'duplicate' / 'unique' / 'aggregate' / 'primary' / 'mv'. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `fe_host` | `str` | — | StarRocks Frontend host (metadata only). |

[//]: # (FIELDS:END)

## Example YAML

```yaml
type: dagster_community_components.ExternalStarRocksTableAsset
attributes:
  asset_key: starrocks/analytics/events_realtime
  database: analytics
  table: events_realtime
  fe_host: starrocks-fe.mycompany.com
  group_name: starrocks
```

## Validation level

**code** — for live validation, see the matching `examples/<topic>.md` walkthrough where available.
