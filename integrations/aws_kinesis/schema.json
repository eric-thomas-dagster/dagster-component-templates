{
  "component_type": "dagster_component_templates.AWSKinesisComponent",
  "description": "Component for importing AWS Kinesis entities as Dagster assets.",
  "category": "integration",
  "icon": "si:amazonkinesis",
  "x-dagster-io": {
    "outputs": {
      "type": "MaterializeResult",
      "description": "Integration result"
    }
  },
  "attributes": {
    "aws_region": {
      "type": "string",
      "label": "Aws Region",
      "description": "AWS region",
      "required": true
    },
    "aws_access_key_id": {
      "type": "string",
      "label": "Aws Access Key Id",
      "description": "Aws Access Key Id",
      "required": false,
      "default": null
    },
    "aws_secret_access_key": {
      "type": "string",
      "label": "Aws Secret Access Key",
      "description": "Aws Secret Access Key",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "aws_session_token": {
      "type": "string",
      "label": "Aws Session Token",
      "description": "Aws Session Token",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "import_firehose_streams": {
      "type": "boolean",
      "label": "Import Firehose Streams",
      "description": "Import Firehose delivery streams as materializable assets",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "import_analytics_applications": {
      "type": "boolean",
      "label": "Import Analytics Applications",
      "description": "Import Data Analytics applications as materializable assets",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "filter_by_name_pattern": {
      "type": "string",
      "label": "Filter By Name Pattern",
      "description": "Regex pattern to filter entities by name",
      "required": false,
      "default": null
    },
    "exclude_name_pattern": {
      "type": "string",
      "label": "Exclude Name Pattern",
      "description": "Regex pattern to exclude entities by name",
      "required": false,
      "default": null
    },
    "filter_by_tags": {
      "type": "string",
      "label": "Filter By Tags",
      "description": "Comma-separated tag keys to filter entities (e.g., ",
      "required": false,
      "default": null
    },
    "poll_interval_seconds": {
      "type": "integer",
      "label": "Poll Interval Seconds",
      "description": "Sensor poll interval in seconds",
      "required": false,
      "default": 60
    },
    "generate_sensor": {
      "type": "boolean",
      "label": "Generate Sensor",
      "description": "Generate observation sensor for delivery streams and applications",
      "required": false,
      "default": true,
      "ui:widget": "checkbox"
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Asset group name for all imported assets",
      "required": false,
      "default": "aws_kinesis"
    },
    "description": {
      "type": "string",
      "label": "Description",
      "description": "Description for the AWS Kinesis component",
      "required": false,
      "default": null,
      "ui:widget": "code"
    }
  },
  "tags": [
    "integration",
    "aws",
    "kinesis"
  ]
}