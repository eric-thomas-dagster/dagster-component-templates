# AWS SageMaker

Imports AWS SageMaker training jobs, batch transform jobs, processing jobs, and pipelines as Dagster assets. Discovers recently completed jobs (last 30 days) as templates for new runs. An optional observation sensor emits `AssetMaterialization` events when training jobs complete.

## Required packages

```
boto3>=1.26.0
botocore>=1.29.0
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `aws_region` | `str` | AWS region |

### Execution

| Field | Type | Default | Description |
|---|---|---|---|
| `poll_interval_seconds` | `int` | `60` | Sensor poll interval in seconds |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `group_name` | `str` | `"aws_sagemaker"` | Asset group name for all imported assets |
| `description` | `str` | — | Description for the AWS SageMaker component |

### Source / target

| Field | Type | Default | Description |
|---|---|---|---|
| `filter_by_name_pattern` | `str` | — | Regex pattern to filter entities by name |
| `filter_by_tags` | `str` | — | Comma-separated tag keys to filter entities (e.g., 'env,team') |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `aws_access_key_id` | `str` | — | AWS access key ID (optional if using IAM role) |
| `aws_secret_access_key` | `str` | — | AWS secret access key (optional if using IAM role) |
| `aws_session_token` | `str` | — | AWS session token for temporary credentials (optional) |
| `import_training_jobs` | `bool` | `true` | Import training job definitions as materializable assets |
| `import_transform_jobs` | `bool` | `true` | Import transform job definitions as materializable assets |
| `import_processing_jobs` | `bool` | `true` | Import processing job definitions as materializable assets |
| `import_pipelines` | `bool` | `true` | Import pipeline definitions as materializable assets |
| `exclude_name_pattern` | `str` | — | Regex pattern to exclude entities by name |
| `generate_sensor` | `bool` | `true` | Generate observation sensor for completed jobs |

[//]: # (FIELDS:END)

## Example YAML

```yaml
type: dagster_component_templates.AWSSageMakerComponent
attributes:
  aws_region: us-east-1
  aws_access_key_id: "{{ env('AWS_ACCESS_KEY_ID') }}"
  aws_secret_access_key: "{{ env('AWS_SECRET_ACCESS_KEY') }}"
  import_training_jobs: true
  import_pipelines: true
  filter_by_name_pattern: "^prod-"
  group_name: aws_sagemaker
```

## Asset Dependencies & Lineage

Because this component enumerates many assets from one config, dependencies are declared per-asset via `asset_overrides` (keyed by the emitted asset's name). Matches the pattern used by the official [`DatabricksWorkspaceComponent`](https://docs.dagster.io/integrations/libraries/databricks/databricks-workspace-component#managing-dependencies).

```yaml
attributes:
  aws_region: us-east-1
  asset_overrides:
    training_job_xgboost_v1:
      depends_on:
        - feature_store/customer_features
        - feature_store/order_history
    pipeline_customer_scoring:
      depends_on:
        - training_job_xgboost_v1
```

The declared deps draw lineage edges in the Dagster asset graph — no data is loaded at runtime. Use them to express that a specific imported SageMaker job or pipeline depends on upstream tables or assets produced elsewhere in your project. Assets without a matching entry in `asset_overrides` are emitted with no upstream deps (the default).

Dependencies can also be wired externally via `map_resolved_asset_specs()` in `definitions.py` — the same approach used by [Dagster Designer](https://github.com/eric-thomas-dagster/dagster_designer).
