{
  "component_type": "dagster_component_templates.DatabricksWorkspaceResource",
  "description": "Resource for interacting with Databricks workspace.",
  "category": "integration",
  "icon": "si:databricks",
  "x-dagster-io": {
    "outputs": {
      "type": "MaterializeResult",
      "description": "Integration result"
    }
  },
  "attributes": {
    "host": {
      "type": "string",
      "label": "Host",
      "description": "Databricks workspace URL",
      "required": true
    },
    "token": {
      "type": "string",
      "label": "Token",
      "description": "Databricks personal access token",
      "required": true,
      "ui:widget": "password"
    },
    "databricks_config_path": {
      "type": "string",
      "label": "Databricks Config Path",
      "description": "Databricks Config Path",
      "required": true
    },
    "workspace_host": {
      "type": "string",
      "label": "Workspace Host",
      "description": "Databricks workspace URL",
      "required": true
    },
    "workspace_token": {
      "type": "string",
      "label": "Workspace Token",
      "description": "Databricks personal access token",
      "required": true,
      "ui:widget": "password"
    },
    "asset_key_prefix": {
      "type": "string",
      "label": "Asset Key Prefix",
      "description": "Optional prefix for all asset keys generated from bundle tasks",
      "required": false,
      "default": null
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "description": "Group name for all generated assets",
      "required": false,
      "default": "databricks_bundle"
    }
  },
  "tags": [
    "integration",
    "databricks",
    "asset",
    "bundle"
  ],
  "name": "Databricks Asset Bundle"
}