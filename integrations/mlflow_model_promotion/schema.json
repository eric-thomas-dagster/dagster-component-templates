{
  "component_type": "dagster_community_components.MLflowModelPromotionComponent",
  "description": "Materializable asset that transitions an MLflow model version between stages (e.g. Staging → Production). Auditable ML CD step in the Dagster asset graph.",
  "category": "integration",
  "attributes": {
    "type": "object",
    "properties": {
      "asset_name": { "type": "string" },
      "tracking_uri_env_var": { "type": "string" },
      "model_name": { "type": "string" },
      "target_stage": { "type": "string", "enum": ["Production", "Staging", "Archived", "None"], "default": "Production" },
      "source_stage": { "type": "string", "enum": ["Production", "Staging", "Archived", "None"], "description": "Promote latest at this stage (mutex with model_version)." },
      "model_version": { "type": "string", "description": "Pin a specific version to promote (mutex with source_stage)." },
      "archive_existing_target": { "type": "boolean", "default": true, "description": "Archive any versions currently at target_stage." },
      "description_prefix": { "type": "string" },
      "group_name": { "type": "string", "default": "mlflow" },
      "asset_key_prefix": { "type": "array", "items": { "type": "string" } },
      "username_env_var": { "type": "string" },
      "password_env_var": { "type": "string" }
    },
    "required": ["asset_name", "tracking_uri_env_var", "model_name"]
  }
}
