{
  "component_type": "dagster_community_components.MLflowWorkspaceComponent",
  "description": "Auto-emit one Dagster asset per MLflow experiment (recent runs + metrics + params) and per registered model (versions). StateBackedComponent — discovery cached to disk. Materializing an experiment asset fetches recent runs; materializing a model asset fetches model version metadata.",
  "category": "integration",
  "icon": "si:mlflow",
  "x-dagster-io": {
    "outputs": {
      "type": "integration",
      "description": "One Dagster asset per selected item discovered in the vendor. See README for what each materialization produces."
    }
  },
  "attributes": {
    "experiment_selector": {
      "type": "object",
      "label": "Experiment Selector",
      "required": false,
      "description": "Fivetran-shape selector against experiment names."
    },
    "model_selector": {
      "type": "object",
      "label": "Model Selector",
      "required": false,
      "description": "Fivetran-shape selector against registered model names."
    },
    "runs_limit": {
      "type": "integer",
      "label": "Runs Limit per Experiment",
      "required": false,
      "default": 100
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": false
    },
    "asset_key_prefix": {
      "type": "array",
      "label": "Asset Key Prefix",
      "required": false,
      "default": [
        "mlflow"
      ]
    },
    "compute_kind": {
      "type": "string",
      "label": "Compute Kind",
      "required": false,
      "default": "mlflow"
    },
    "defs_state": {
      "type": "object",
      "label": "Defs State Config",
      "required": false
    }
  }
}
