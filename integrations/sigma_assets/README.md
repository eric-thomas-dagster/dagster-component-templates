# SigmaAssets

Wraps `dagster-sigma`'s `SigmaOrganization` to surface Sigma workbooks and dataset definitions as Dagster external assets — they show up in the asset graph alongside their upstream warehouse tables, giving you BI-tier lineage.

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `base_url` | `str` | Sigma base URL (e.g. 'https://aws-api.sigmacomputing.com'). |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `client_id_env_var` | `str` | `"SIGMA_CLIENT_ID"` | Env var with the Sigma client_id. |
| `client_secret_env_var` | `str` | `"SIGMA_CLIENT_SECRET"` | Env var with the Sigma client_secret. |

[//]: # (FIELDS:END)

## Example YAML
```yaml
type: dagster_component_templates.SigmaAssetsComponent
attributes:
  base_url: <fill in>
  client_id_env_var: <fill in>
  client_secret_env_var: <fill in>
```

## Requirements

```
dagster
dagster-sigma
```
