type: dagster_component_templates.SlingReplicationAssetComponent
attributes:
  # Path to a Sling replication YAML file (relative to project root).
  replication_config_path: sling/replication.yaml
  group_name: sling
  name: sling_replication
