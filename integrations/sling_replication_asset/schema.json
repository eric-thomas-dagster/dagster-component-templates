{
  "component_type": "dagster_component_templates.SlingReplicationAssetComponent",
  "description": "Run a Sling YAML replication as Dagster assets via dagster-sling.",
  "category": "ingestion",
  "icon": "ArrowRightLeft",
  "x-dagster-io": {
    "outputs": {
      "type": "MaterializeResult",
      "description": "One or more replicated tables materialized via Sling -- no data passed through the IO manager."
    }
  },
  "attributes": {
    "replication_config_path": {
      "type": "string",
      "label": "Replication Config Path",
      "required": true
    },
    "group_name": {
      "type": "string",
      "label": "Group Name",
      "required": true
    },
    "name": {
      "type": "string",
      "label": "Name",
      "required": true
    }
  },
  "tags": [
    "integration",
    "sling",
    "elt",
    "official"
  ],
  "name": "SlingReplicationAsset",
  "vertical": "database"
}
