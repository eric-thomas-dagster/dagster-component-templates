{
  "component_type": "dagster_component_templates.SnowflakeAlertComponent",
  "description": "Define a Snowflake ALERT (scheduled conditional action). Materialization runs CREATE OR REPLACE ALERT and resumes it.",
  "category": "integration",
  "icon": "si:snowflake",
  "x-dagster-io": {
    "outputs": {
      "type": "integration",
      "description": "One Dagster asset per selected item discovered in the vendor. See README for what each materialization produces."
    }
  },
  "attributes": {
    "asset_name": {"label": "Asset Name", 
      "type": "string",
      "required": true
    },
    "account": {"label": "Account", 
      "type": "string",
      "required": true
    },
    "user": {"label": "User", 
      "type": "string",
      "required": true
    },
    "database": {"label": "Database", 
      "type": "string",
      "required": true
    },
    "schema_name": {"label": "Schema Name", 
      "type": "string",
      "required": true
    },
    "warehouse": {"label": "Warehouse", 
      "type": "string"
    },
    "role": {"label": "Role", 
      "type": "string"
    },
    "password": {"label": "Password", 
      "type": "string"
    },
    "authenticator": {"label": "Authenticator", 
      "type": "string",
      "description": "SNOWFLAKE_JWT | externalbrowser | PROGRAMMATIC_ACCESS_TOKEN | oauth",
      "enum": [
        "SNOWFLAKE_JWT",
        "externalbrowser",
        "PROGRAMMATIC_ACCESS_TOKEN",
        "oauth"
      ],
      "ui:widget": "select"
    },
    "private_key_file": {"label": "Private Key File", 
      "type": "string"
    },
    "private_key_file_pwd": {"label": "Private Key File Pwd", 
      "type": "string"
    },
    "token": {"label": "Token", 
      "type": "string"
    },
    "group_name": {"label": "Group Name", 
      "type": "string"
    },
    "description": {"label": "Description", 
      "type": "string"
    },
    "asset_tags": {"label": "Asset Tags", 
      "type": "object"
    },
    "kinds": {"label": "Kinds", 
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "owners": {"label": "Owners", 
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "deps": {"label": "Deps", 
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
