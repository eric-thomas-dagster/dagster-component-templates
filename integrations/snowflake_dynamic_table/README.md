# Snowflake Dynamic Table

Define a Snowflake DYNAMIC TABLE as Dagster YAML. Materialization runs CREATE OR REPLACE DYNAMIC TABLE.

## Why this component

Defines a Snowflake `DYNAMIC TABLE` as code. Dynamic tables are Snowflake's auto-refreshing materialized views with declarative `TARGET_LAG`. This component lets you express their DDL in `defs.yaml`; Dagster issues `CREATE OR REPLACE DYNAMIC TABLE` on materialize.

Defaults to `INITIALIZE = ON_SCHEDULE` to avoid privilege issues with immediate refresh. The DT then refreshes on its own schedule per `TARGET_LAG`.

### Complementary to `snowflake_workspace`

| Component | Role |
|---|---|
| [`snowflake_workspace`](https://raw.githubusercontent.com/eric-thomas-dagster/dagster-component-templates/main/integrations/snowflake_workspace/README.md) | **Discovers** existing Snowflake entities in your account → imports them as Dagster assets |
| **`snowflake_dynamic_table`** (this) | **Defines** a new entity in Dagster YAML → Dagster issues CREATE OR REPLACE on materialize |

Use whichever side matches the source of truth for each entity. Mix freely — both styles produce ordinary Dagster assets and can declare deps on each other.

<!-- FIELDS:START - auto-generated by tools/regen_readme_fields.py -->

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `asset_name` | `str` | Output Dagster asset name |
| `account` | `str` | Snowflake account, e.g. xy12345-abc. |
| `user` | `str` | Snowflake user (NAME for keypair, LOGIN_NAME for password/SSO/PAT). |
| `database` | `str` | Database containing the entity (or where it will be created). |
| `schema_name` | `str` | Schema for the entity. |
| `dt_name` | `str` | Dynamic table name (unquoted identifier) |
| `target_lag` | `str` | "5 minutes" / "1 hour" / "DOWNSTREAM" |
| `sql` | `str` | The dynamic table's SELECT statement. |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `warehouse` | `str` | — | — |
| `role` | `str` | — | — |
| `password` | `str` | — | Snowflake password. Leave unset for SSO/keypair/PAT. |
| `authenticator` | `str` | — | Snowflake authenticator: 'SNOWFLAKE_JWT' (keypair), 'externalbrowser' (SSO), 'PROGRAMMATIC_ACCESS_TOKEN' (PAT), 'oauth'. |
| `private_key_file` | `str` | — | PEM private key file path (for SNOWFLAKE_JWT). |
| `private_key_file_pwd` | `str` | — | Passphrase for encrypted private_key_file. |
| `token` | `str` | — | OAuth/PAT token (for PROGRAMMATIC_ACCESS_TOKEN or oauth). |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `group_name` | `str` | — | — |
| `description` | `str` | — | — |
| `asset_tags` | `Dict[str, str]` | — | — |
| `kinds` | `List[str]` | — | — |
| `owners` | `List[str]` | — | — |
| `deps` | `List[str]` | — | — |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `initialize` | `str` | `"ON_SCHEDULE"` | 'ON_CREATE' or 'ON_SCHEDULE'. Default ON_SCHEDULE avoids immediate-refresh privilege issues. |
| `refresh_mode` | `str` | `"AUTO"` | 'AUTO' / 'INCREMENTAL' / 'FULL' |
| `cluster_by` | `List[str]` | — | Columns to CLUSTER BY for performance. |

<!-- FIELDS:END -->

## Typical use cases

- Define declarative auto-refreshing views as code
- Pair with snowflake_workspace to mix imported + defined entities in one project
- Set TARGET_LAG / REFRESH_MODE / CLUSTER BY from YAML

## Install

```bash
uvx --from dagster-community-components-cli dagster-component add snowflake_dynamic_table --auto-install
```

## Minimal config

See [`example.yaml`](https://raw.githubusercontent.com/eric-thomas-dagster/dagster-component-templates/main/integrations/snowflake_dynamic_table/example.yaml) for a copy-pastable starting point.

```yaml
type: dagster_component_templates.SnowflakeDynamicTableComponent
attributes:
  asset_name: paid_orders_dt
  dt_name: PAID_ORDERS_DT
  account: "{{ env('SNOWFLAKE_ACCOUNT') }}"
  user:    "{{ env('SNOWFLAKE_USER') }}"
  authenticator: SNOWFLAKE_JWT
  private_key_file: "{{ env('SNOWFLAKE_PRIVATE_KEY_FILE') }}"
  warehouse: COMPUTE_WH
  database: DAGSTER_DEMO
  schema_name: STAGING
  target_lag: "5 minutes"
  initialize: ON_SCHEDULE
  sql: |
    SELECT * FROM DAGSTER_DEMO.RAW.ORDERS WHERE STATUS IN ('paid', 'delivered')
  group_name: snowflake_ops
```

## Schema

Every connection field (account / user / warehouse / database / schema_name / role) is required for runtime; the auth method varies:

| Auth | What to set |
|---|---|
| Password | `password: "{{ env('SNOWFLAKE_PASSWORD') }}"` |
| SSO | `authenticator: externalbrowser` (no other auth field) |
| Keypair (recommended for headless) | `authenticator: SNOWFLAKE_JWT` + `private_key_file: "{{ env('SNOWFLAKE_PRIVATE_KEY_FILE') }}"` (+ `private_key_file_pwd` if encrypted) |
| PAT | `authenticator: PROGRAMMATIC_ACCESS_TOKEN` + `token: "{{ env('SNOWFLAKE_PAT') }}"` |

See [`schema.json`](https://raw.githubusercontent.com/eric-thomas-dagster/dagster-component-templates/main/integrations/snowflake_dynamic_table/schema.json) for the full attribute list.

## Privileges required on the connecting role

| Operation | Privilege |
|---|---|
| Create the entity | `CREATE DYNAMIC TABLE` on the target schema |
| Read referenced tables | `SELECT` on source tables |
| For tasks / alerts: trigger execution | `EXECUTE TASK` (or `EXECUTE ALERT`) on account |
| For dynamic tables: run refresh | `OPERATE` on the warehouse |

## See also

- [`snowflake_workspace`](https://raw.githubusercontent.com/eric-thomas-dagster/dagster-component-templates/main/integrations/snowflake_workspace) — import existing entities
- The end-to-end walkthrough: [Snowflake Workspace → Dagster](https://github.com/eric-thomas-dagster/dagster-community-components-cli/blob/main/examples/snowflake_workspace.md)