name: Snowflake Stream
description: Define a Snowflake STREAM (CDC) as Dagster YAML. Materialization runs CREATE OR REPLACE STREAM ON TABLE.
category: integration
version: 1.0.0
author: Dagster Community
