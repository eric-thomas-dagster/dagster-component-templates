# BigQuery IO Manager

Register a BigQueryPandasIOManager so assets are automatically stored in and loaded from BigQuery.

## Installation

```
pip install dagster-gcp-pandas
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `project` | `str` | GCP project ID |
| `dataset` | `str` | BigQuery dataset to store assets in |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"io_manager"` | Dagster resource key for this IO manager. Use 'io_manager' to make it the default. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `location` | `str` | — | GCP region, e.g. 'US', 'EU', 'us-central1' |
| `gcp_credentials_env_var` | `str` | — | Environment variable holding a JSON service account key (optional; uses ADC if not set) |
| `temporary_gcs_bucket` | `str` | — | GCS bucket for temporary files during load jobs |

[//]: # (FIELDS:END)

## Configuration

```yaml
type: dagster_component_templates.BigQueryIOManagerComponent
attributes:
  resource_key: io_manager
  project: my-gcp-project
  dataset: dagster_assets
  location: US
```

## Authentication

Uses Application Default Credentials (ADC) by default. Set `gcp_credentials_env_var` to a JSON service account key if needed:

```bash
export GCP_CREDENTIALS='{"type": "service_account", ...}'
```