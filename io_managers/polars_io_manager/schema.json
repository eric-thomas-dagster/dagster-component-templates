{
  "component_type": "dagster_component_templates.PolarsIOManagerComponent",
  "description": "Wrap the official `dagster-polars` PolarsParquetIOManager so polars.DataFrame assets persist as Parquet.",
  "category": "io_manager",
  "icon": "Save",
  "x-dagster-io": {
    "outputs": {
      "type": "IOManager",
      "description": "Wrap the official `dagster-polars` PolarsParquetIOManager so polars.DataFrame assets persist as Parquet."
    }
  },
  "attributes": {
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "default": "io_manager",
      "required": false
    },
    "base_dir": {
      "type": "string",
      "label": "Base Dir",
      "required": true
    }
  },
  "tags": [
    "io_manager",
    "polars",
    "parquet",
    "official"
  ],
  "name": "PolarsIOManager"
}
