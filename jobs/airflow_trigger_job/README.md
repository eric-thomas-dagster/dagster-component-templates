# AirflowTriggerJobComponent

Trigger an Airflow DAG run via the Airflow REST API as a Dagster job.

## Dependencies
- `requests`

## What this is
A **Dagster job** (no asset materialized) — the component installs a `dg.job`
plus an optional schedule. Lineage graph stays clean; the work happens
behind the scenes.

## See also
- [Schema](schema.json) · [Example config](example.yaml)

<!-- FIELDS:START - auto-generated by tools/regen_readme_fields.py -->

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `job_name` | `str` | Dagster job name |
| `airflow_url` | `str` | Airflow web URL (e.g. 'https://airflow.acme.com') |
| `dag_id` | `str` | DAG to trigger |

### Execution

| Field | Type | Default | Description |
|---|---|---|---|
| `wait_for_completion` | `bool` | `false` | — |
| `poll_interval_seconds` | `int` | `15` | — |
| `timeout_seconds` | `int` | `3600` | — |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `tags` | `dict` | — | Dagster job tags |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `default_status` | `str` | `"STOPPED"` | STOPPED \| RUNNING |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `schedule` | `str` | — | Cron schedule (None = no schedule) |
| `username_env` | `str` | `"AIRFLOW_USER"` | — |
| `password_env` | `str` | `"AIRFLOW_PASSWORD"` | — |
| `conf` | `dict` | — | DAG run conf payload |

<!-- FIELDS:END -->
