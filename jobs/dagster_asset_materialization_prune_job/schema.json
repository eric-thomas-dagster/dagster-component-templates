{
  "component_type": "dagster_component_templates.DagsterAssetMaterializationPruneJobComponent",
  "description": "Op-shaped job that wipes asset materialization history for configured assets, preserving the last N via synthetic re-emission tagged _re_emitted_by_prune_job=true.",
  "category": "jobs",
  "icon": "Trash",
  "attributes": {
    "job_name": {"label": "Job Name", "type": "string", "required": true},
    "schedule": {"label": "Schedule", "type": "string", "required": false, "default": "0 5 * * 0", "x-dagster-widget": "cron"},
    "default_status": {"label": "Default Status", "type": "string", "required": false, "default": "stopped", "enum": ["stopped", "running"]},
    "tags": {"label": "Tags", "type": "object", "required": false, "default": null},
    "asset_keys": {"type": "array", "label": "Asset Keys", "description": "Asset user-strings to prune. Each history is wiped; last N re-emitted synthetically.", "required": true, "items": {"type": "string"}},
    "keep_last_n_per_asset": {"type": "integer", "label": "Keep Last N Per Asset", "description": "Re-emitted synthetically after wipe. Set 0 to wipe everything.", "required": false, "default": 100},
    "dry_run": {"label": "Dry Run", "type": "boolean", "required": false, "default": true}
  }
}
