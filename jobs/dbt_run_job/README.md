# DbtRunJobComponent

Run a `dbt` command (run, build, test, snapshot) as a Dagster job — no asset modeling.

## Dependencies
- `dbt-core`

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `job_name` | `str` | Dagster job name |
| `project_dir` | `str` | Path to dbt project root (where dbt_project.yml lives) |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `tags` | `dict` | — | Dagster job tags |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `default_status` | `str` | `"STOPPED"` | STOPPED \| RUNNING |
| `target` | `str` | — | dbt target name (overrides default) |

### Source / target

| Field | Type | Default | Description |
|---|---|---|---|
| `select` | `str` | — | --select expression |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `schedule` | `str` | — | Cron schedule (None = no schedule) |
| `profiles_dir` | `str` | — | Path to profiles.yml (default: ~/.dbt) |
| `command` | `str` | `"run"` | dbt subcommand: run \| build \| test \| snapshot \| seed \| source |
| `exclude` | `str` | — | --exclude expression |
| `full_refresh` | `bool` | `false` | — |
| `vars` | `dict` | — | --vars passed as JSON |

[//]: # (FIELDS:END)

## What this is
A **Dagster job** (no asset materialized) — the component installs a `dg.job`
plus an optional schedule. Lineage graph stays clean; the work happens
behind the scenes.

## See also
- [Schema](schema.json) · [Example config](example.yaml)