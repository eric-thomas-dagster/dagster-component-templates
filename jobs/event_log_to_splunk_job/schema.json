{
  "component_type": "dagster_component_templates.EventLogToSplunkJobComponent",
  "description": "Op-shaped job that ships new Dagster event log entries to Splunk via the HTTP Event Collector (HEC). Simpler than dagster_plus_to_siem_job: OSS event log only, single-destination.",
  "category": "jobs",
  "icon": "Upload",
  "attributes": {
    "job_name": {"label": "Job Name", "type": "string", "required": true},
    "schedule": {"label": "Schedule", "type": "string", "required": false, "default": "*/5 * * * *", "x-dagster-widget": "cron"},
    "default_status": {"label": "Default Status", "type": "string", "required": false, "default": "stopped", "enum": ["stopped", "running"]},
    "tags": {"label": "Tags", "type": "object", "required": false, "default": null},
    "hec_url": {"type": "string", "label": "Splunk HEC URL", "required": true, "default": "https://splunk.example.com:8088/services/collector/event"},
    "hec_token_env": {"label": "Hec Token Env", "type": "string", "required": false, "default": "SPLUNK_HEC_TOKEN"},
    "sourcetype": {"label": "Sourcetype", "type": "string", "required": false, "default": "dagster:event"},
    "index": {"label": "Index", "type": "string", "required": false, "default": null},
    "verify_ssl": {"label": "Verify Ssl", "type": "boolean", "required": false, "default": true},
    "initial_lookback_hours": {"label": "Initial Lookback Hours", "type": "integer", "required": false, "default": 6},
    "batch_limit": {"label": "Batch Limit", "type": "integer", "required": false, "default": 5000}
  }
}
