# ObservabilityHeartbeatJobComponent

Compound op job: send a heartbeat to multiple destinations (Slack + PagerDuty + HTTP) on cron — single YAML config.

## Dependencies
- `requests`

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `job_name` | `str` | Dagster job name |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `default_status` | `str` | `"STOPPED"` | — |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `schedule` | `str` | — | Cron schedule |
| `message` | `str` | `"dagster heartbeat"` | Common message body |
| `slack_webhook_url_env` | `str` | — | Slack webhook URL env var |
| `pagerduty_routing_key_env` | `str` | — | PD routing key env var |
| `http_heartbeat_url` | `str` | — | GET this URL (e.g. UptimeRobot) |
| `teams_webhook_url_env` | `str` | — | Teams webhook URL env var |

[//]: # (FIELDS:END)

## What this is
A **Dagster job** (no asset materialized) — the component installs a `dg.job`
plus an optional schedule. Lineage graph stays clean; the work happens
behind the scenes.

## See also
- [Schema](schema.json) · [Example config](example.yaml)