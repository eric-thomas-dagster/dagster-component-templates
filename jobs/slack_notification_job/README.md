# SlackNotificationJobComponent

Send a Slack message as a scheduled job — daily summaries, weekly reports, ad-hoc alerts.

## Dependencies
- `requests`

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `job_name` | `str` | Dagster job name |
| `message` | `str` | Message text (supports Slack markdown) |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `username` | `str` | — | — |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `tags` | `dict` | — | Dagster job tags |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `default_status` | `str` | `"STOPPED"` | STOPPED \| RUNNING |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `schedule` | `str` | — | Cron schedule (None = no schedule) |
| `webhook_url_env` | `str` | `"SLACK_WEBHOOK_URL"` | Env var with Slack incoming-webhook URL |
| `channel` | `str` | — | Override channel (e.g. '#data-platform') |
| `icon_emoji` | `str` | `":robot_face:"` | — |

[//]: # (FIELDS:END)

## What this is
A **Dagster job** (no asset materialized) — the component installs a `dg.job`
plus an optional schedule. Lineage graph stays clean; the work happens
behind the scenes.

## See also
- [Schema](schema.json) · [Example config](example.yaml)