# SqlCommandJobComponent

Execute a SQL maintenance command (VACUUM, ANALYZE, REFRESH MATERIALIZED VIEW, etc.) — no asset.

## Dependencies
- `sqlalchemy`

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `job_name` | `str` | Dagster job name |
| `sql` | `str` | SQL statement to execute (one or many, separated by semicolons) |

### Catalog metadata

| Field | Type | Default | Description |
|---|---|---|---|
| `tags` | `dict` | — | Dagster job tags |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `default_status` | `str` | `"STOPPED"` | STOPPED \| RUNNING |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `schedule` | `str` | — | Cron schedule (None = no schedule) |
| `connection_string_env` | `str` | `"DATABASE_URL"` | Env var with SQLAlchemy URL |
| `autocommit` | `bool` | `true` | Required for VACUUM, certain DDL on Postgres |

[//]: # (FIELDS:END)

## What this is
A **Dagster job** (no asset materialized) — the component installs a `dg.job`
plus an optional schedule. Lineage graph stays clean; the work happens
behind the scenes.

## See also
- [Schema](schema.json) · [Example config](example.yaml)