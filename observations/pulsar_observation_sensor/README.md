# Pulsar Observation Sensor

Emits `AssetObservation` health metrics for an `ExternalPulsarAsset` via the Pulsar Admin REST API.

**Metrics**: producer_count, subscription_count, msg_rate_in, storage_size_bytes
