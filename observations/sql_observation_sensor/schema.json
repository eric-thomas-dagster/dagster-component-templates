{
  "component_type": "dagster_component_templates.SqlObservationSensorComponent",
  "description": "Periodically connect to SQL and record an AssetObservation on the target external asset — surfaces health metrics in the Dagster UI without triggering a pipeline run.",
  "category": "observation",
  "icon": "Eye",
  "x-dagster-io": {
    "outputs": {
      "type": "AssetObservation",
      "description": "Asset observation events"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique sensor name",
      "required": true
    },
    "asset_key": {
      "type": "string",
      "label": "Asset Key",
      "description": "Asset key of the ExternalSqlAsset to observe",
      "required": true
    },
    "table_name": {
      "type": "string",
      "label": "Table Name",
      "description": "Table name to observe",
      "required": true
    },
    "connection_string_env_var": {
      "type": "string",
      "label": "Connection String Env Var",
      "description": "Env var with SQLAlchemy connection string",
      "required": true
    },
    "watermark_column": {
      "type": "string",
      "label": "Watermark Column",
      "description": "Column for latest value metric (e.g. updated_at). Used to derive the DataVersion in the native path.",
      "required": false,
      "default": null,
      "x-dagster-widget": "column"
    },
    "check_interval_seconds": {
      "type": "integer",
      "label": "Check Interval Seconds",
      "description": "Seconds between health checks",
      "required": false,
      "default": 300
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key.",
      "required": false,
      "default": null,
      "ui:widget": "resource-picker"
    }
  },
  "tags": [
    "observation",
    "sql",
    "sensor"
  ],
  "name": "SQL Observation Sensor"
}
