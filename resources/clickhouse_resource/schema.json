{
  "component_type": "dagster_component_templates.ClickHouseResourceComponent",
  "description": "Register a ClickHouse resource for use by other components.\n\nExample:\n\n    ```yaml\n    type: dagster_community_components.ClickHouseResourceComponent\n    attributes:\n      resource_key: clickhouse_resource\n      host: clickhouse.mycompany.com\n      port: 8443\n      database: analytics\n      username: dagster_runner\n      password_env_var: CLICKHOUSE_PASSWORD\n      secure: true\n    ```\n\nPairs with:\n  - ``clickhouse_io_manager`` — Pandas asset persistence\n  - ``dataframe_to_clickhouse`` — bulk insert\n  - ``external_clickhouse_table`` — catalog declaration\n  - Generic SQL components via ``connection_string``",
  "category": "resource",
  "icon": "si:clickhouse",
  "attributes": {
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "required": false,
      "description": "Resource key.",
      "default": "clickhouse_resource"
    },
    "host": {
      "type": "string",
      "label": "Host",
      "required": true,
      "description": "ClickHouse server hostname."
    },
    "port": {
      "type": "integer",
      "label": "Port",
      "required": false,
      "description": "HTTP port (8123 plain, 8443 TLS). For native TCP use 9000 + a different client.",
      "default": 8123
    },
    "database": {
      "type": "string",
      "label": "Database",
      "required": false,
      "description": "Database name.",
      "default": "default"
    },
    "username": {
      "type": "string",
      "label": "Username",
      "required": false,
      "description": "ClickHouse username.",
      "default": "default"
    },
    "password": {
      "type": "string",
      "label": "Password",
      "required": false,
      "description": "Password. Set this OR password_env_var.",
      "default": null
    },
    "password_env_var": {
      "type": "string",
      "label": "Password Env Var",
      "required": false,
      "description": "Env var with password.",
      "default": null
    },
    "secure": {
      "type": "boolean",
      "label": "Secure",
      "required": false,
      "description": "Enable TLS.",
      "default": false
    }
  }
}
