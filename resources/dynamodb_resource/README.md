# AWS DynamoDB Resource

Register an AWS DynamoDB resource for serverless key-value and document storage

A Dagster resource component that provides a DynamoDB `ConfigurableResource` backed by `boto3`.

## Installation

```
pip install boto3
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `region_name` | `str` | AWS region, e.g. 'us-east-1' |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"dynamodb_resource"` | Key used to register this resource. Other components reference it via resource_key. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `aws_access_key_id_env_var` | `str` | — | Environment variable holding the AWS access key ID. Leave empty to use boto3's default credential chain (IAM role, ~/.aws, etc.). |
| `aws_secret_access_key_env_var` | `str` | — | Environment variable holding the AWS secret access key. |
| `endpoint_url` | `str` | — | Custom DynamoDB endpoint URL for LocalStack or DynamoDB Local, e.g. 'http://localhost:8000'. |

[//]: # (FIELDS:END)

## Configuration

```yaml
type: dynamodb_resource.component.DynamoDBResourceComponent
attributes:
  resource_key: dynamodb_resource
  region_name: us-east-1
  aws_access_key_id_env_var: AWS_ACCESS_KEY_ID
  aws_secret_access_key_env_var: AWS_SECRET_ACCESS_KEY
```

## Auth

Set the environment variables named in `aws_access_key_id_env_var` and `aws_secret_access_key_env_var` to your AWS credentials. If omitted, boto3 will fall back to its standard credential chain (IAM roles, `~/.aws/credentials`, etc.). Use `endpoint_url` to point at DynamoDB Local for local development.