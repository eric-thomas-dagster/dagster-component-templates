# Elasticsearch Resource

Register an Elasticsearch resource for full-text search and analytics

A Dagster resource component that provides an Elasticsearch `ConfigurableResource` backed by the official `elasticsearch` Python client.

## Installation

```
pip install "elasticsearch>=8.0.0"
```

## Configuration

```yaml
type: elasticsearch_resource.component.ElasticsearchResourceComponent
attributes:
  resource_key: elasticsearch_resource
  hosts: "https://my-cluster.es.io:9243"
  api_key_env_var: ELASTICSEARCH_API_KEY
  verify_certs: true
```

## Auth

Provide either `api_key_env_var` (preferred for Elastic Cloud) or `username` + `password_env_var` for basic auth. Set the referenced environment variables to their respective secret values before running.

<!-- FIELDS:START - auto-generated by tools/regen_readme_fields.py -->

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `hosts` | `str` | Elasticsearch URL, e.g. 'https://localhost:9200' |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"elasticsearch_resource"` | Key used to register this resource. Other components reference it via resource_key. |
| `api_key` | `str` | — | Elasticsearch API key (literal). Set this OR api_key_env_var. Mutually exclusive with username/password. |
| `api_key_env_var` | `str` | — | Env var holding the Elasticsearch API key. Set this OR api_key. |
| `username` | `str` | — | Elasticsearch username for basic auth |
| `password` | `str` | — | Elasticsearch password (literal). Set this OR password_env_var. |
| `password_env_var` | `str` | — | Env var holding the Elasticsearch password. Set this OR password. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `verify_certs` | `bool` | `true` | Verify TLS certificates. Set to False for self-signed dev clusters only. |

<!-- FIELDS:END -->
