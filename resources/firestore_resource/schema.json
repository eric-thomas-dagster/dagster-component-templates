{
  "x-dagster-io": {},
  "x-dagster-provides": [
    "firestore_resource"
  ],
  "category": "resource",
  "type": "object",
  "properties": {
    "resource_key": {
      "type": "string",
      "default": "firestore_resource",
      "description": "The key under which the Firestore resource is registered."
    },
    "project": {
      "type": "string",
      "description": "Google Cloud project ID."
    },
    "gcp_credentials_env_var": {
      "type": "string",
      "description": "Name of the environment variable holding the service account JSON string.",
      "ui:widget": "password"
    },
    "database": {
      "type": "string",
      "default": "(default)",
      "description": "Firestore database ID (use '(default)' for the default database)."
    }
  },
  "required": [
    "project"
  ],
  "description": "Register a Google Cloud Firestore resource for serverless document database access",
  "tags": [
    "resource",
    "firestore",
    "gcp",
    "database",
    "nosql"
  ],
  "icon": "si:firebase",
  "name": "Google Firestore Resource",
  "attributes": {
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "required": false,
      "default": "firestore_resource"
    },
    "project": {
      "type": "string",
      "label": "Project",
      "required": true
    },
    "gcp_credentials_env_var": {
      "type": "string",
      "label": "Gcp Credentials Env Var",
      "required": false
    },
    "database": {
      "type": "string",
      "label": "Database",
      "required": false,
      "default": "(default"
    }
  }
}
