{
  "component_type": "dagster_component_templates.GCSResourceComponent",
  "description": "Register a dagster-gcp GCSResource for use by other components",
  "category": "resource",
  "icon": "si:googlecloudstorage",
  "x-dagster-io": {},
  "x-dagster-provides": ["gcs_resource"],
  "attributes": {
    "resource_key": {
      "type": "string", "label": "Resource Key",
      "description": "Key used to register this resource. Other components reference it via resource_key.",
      "required": false, "default": "gcs_resource"
    },
    "project": {
      "type": "string", "label": "Project",
      "description": "GCP project ID",
      "required": true
    },
    "gcp_credentials_env_var": {
      "type": "string", "label": "GCP Credentials Env Var",
      "description": "Env var containing GCP service account JSON string",
      "required": false, "default": null,
      "ui:widget": "password"
    }
  },
  "tags": ["resource", "gcp", "gcs"]
}
