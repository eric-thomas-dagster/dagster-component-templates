{
  "component_type": "dagster_component_templates.IcebergCatalogResourceComponent",
  "description": "Register a PyIceberg catalog (REST / Glue / Hive / Hadoop / SQL) once and reuse across many ingestion / sink / observation components.",
  "category": "resource",
  "icon": "si:apacheiceberg",
  "attributes": {
    "resource_key": {"type": "string", "label": "Resource Key", "required": false, "default": "iceberg"},
    "catalog_type": {"type": "string", "label": "Catalog Type", "required": false, "default": "rest"},
    "catalog_name": {"type": "string", "label": "Catalog Name", "required": false, "default": "default"},
    "catalog_properties": {"type": "object", "label": "Catalog Properties", "required": false, "ui:widget": "key-value"}
  },
  "tags": ["resource", "iceberg", "lakehouse", "pyiceberg"],
  "name": "Iceberg Catalog Resource"
}
