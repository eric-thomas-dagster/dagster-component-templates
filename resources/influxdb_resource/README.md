# influxdb_resource

## Purpose

InfluxDB 2.x / 3.x connection resource via the official `influxdb-client` SDK. Targets the modern token + org + bucket auth model.

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `url` | `str` | InfluxDB base URL. |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"influxdb_resource"` | Resource key. |
| `token` | `str` | — | API token. Set this OR token_env_var. |
| `token_env_var` | `str` | — | Env var with API token. |

### Source / target

| Field | Type | Default | Description |
|---|---|---|---|
| `bucket` | `str` | — | Default destination bucket. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `org` | `str` | — | InfluxDB organization (required for v2). |
| `timeout_ms` | `int` | `30000` | — |

[//]: # (FIELDS:END)

## Example YAML

```yaml
type: dagster_community_components.InfluxDBResourceComponent
attributes:
  resource_key: influxdb_resource
  url: http://localhost:8086
  token_env_var: INFLUXDB_TOKEN
  org: my-org
  bucket: metrics
```

## Validation level

**code** — see the matching `vendors/<name>.md` page for live demo status.
