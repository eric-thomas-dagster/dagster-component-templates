# Sftp Resource

Registers an SFTP resource for reading and writing files over SSH in your Dagster project.

## Install

```
pip install paramiko
```

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `host` | `str` | SFTP server hostname or IP |
| `username` | `str` | SSH username |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"sftp_resource"` | Key used to register this resource. Other components reference it via resource_key. |
| `port` | `int` | `22` | SFTP port |
| `password_env_var` | `str` | — | Environment variable holding the SSH password |
| `private_key_env_var` | `str` | — | Environment variable holding the SSH private key PEM string |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `private_key_path` | `str` | — | Path to SSH private key file on disk |
| `known_hosts_path` | `str` | — | Path to a known_hosts file. If empty, the system known_hosts (~/.ssh/known_hosts) is loaded. |
| `auto_accept_unknown_host_key` | `bool` | `false` | If True, automatically accept unknown host keys (insecure — enables MITM). Default False rejects unknown hosts. |

[//]: # (FIELDS:END)

## Configuration

```yaml
type: dagster_component_templates.SFTPResourceComponent
attributes:
  resource_key: sftp_resource
  host: sftp.example.com
  username: deploy
  port: 22                              # optional, default: 22
  password_env_var: SFTP_PASSWORD       # optional
  private_key_env_var: SFTP_PKEY_PEM   # optional
  private_key_path: /path/to/key.pem   # optional
```

## Auth

Provide exactly one of: `password_env_var`, `private_key_env_var` (PEM string stored in an env var), or `private_key_path` (path to a key file on disk). Key-based auth is recommended for production.

```
export SFTP_PASSWORD=<password>
# or
export SFTP_PKEY_PEM="$(cat ~/.ssh/id_rsa)"
```