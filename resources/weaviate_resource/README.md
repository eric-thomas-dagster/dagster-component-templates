# Weaviate Resource

Register a dagster-weaviate WeaviateResource for use by other components

## Installation

`pip install dagster-weaviate`

## Configuration

```yaml
type: dagster_component_templates.WeaviateResourceComponent
attributes:
  resource_key: weaviate_resource  # key other components use
  # Optional
```

## Usage in other components

Once placed on the canvas, other components can reference this resource:

```yaml
attributes:
  resource_key: weaviate_resource
```

## Authentication

All secret fields accept an **environment variable name** (not the secret value itself).
Set the corresponding environment variable before running Dagster:

```bash
export API_KEY_ENV_VAR_VALUE="your-secret-here"
```


<!-- FIELDS:START - auto-generated by tools/regen_readme_fields.py -->

## Fields

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `resource_key` | `str` | `"weaviate_resource"` | Key used to register this resource. Other components reference it via resource_key. |
| `url` | `str` | — | Weaviate instance URL for cloud or local, e.g. 'https://my-cluster.weaviate.network' |
| `api_key_env_var` | `str` | — | Environment variable holding the Weaviate API key (for Weaviate Cloud) |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `connection_params` | `Dict` | — | Weaviate connection parameters dict passed to weaviate.connect_to_custom() |

<!-- FIELDS:END -->
