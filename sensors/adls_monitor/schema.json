{
  "component_type": "dagster_component_templates.ADLSMonitorSensorComponent",
  "description": "Component for monitoring Azure Data Lake Storage Gen2 for new files.",
  "category": "sensor",
  "icon": "si:microsoftazure",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor",
      "required": true
    },
    "storage_account_name": {
      "type": "string",
      "label": "Storage Account Name",
      "description": "Azure Storage account name (e.g., ",
      "required": true
    },
    "container_name": {
      "type": "string",
      "label": "Container Name",
      "description": "Container Name",
      "required": true
    },
    "directory_path": {
      "type": "string",
      "label": "Directory Path",
      "description": "Directory path within the container to monitor (e.g., ",
      "required": false,
      "default": ""
    },
    "file_pattern": {
      "type": "string",
      "label": "File Pattern",
      "description": "Regex pattern to match file names (e.g., ",
      "required": false,
      "default": ".*"
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "description": "Name of the job to trigger when files are detected",
      "required": true
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum Interval Seconds",
      "required": false,
      "default": 30
    },
    "recursive": {
      "type": "boolean",
      "label": "Recursive",
      "description": "Whether to recursively list files in subdirectories",
      "required": false,
      "default": false,
      "ui:widget": "checkbox"
    },
    "connection_string_env_var": {
      "type": "string",
      "label": "Connection String Env Var",
      "description": "Name of the environment variable containing the storage connection string. ",
      "required": false,
      "default": null
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key providing a pre-configured client. ",
      "required": false,
      "default": null,
      "ui:widget": "resource-picker"
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "description": "Default Status",
      "required": false,
      "default": "running"
    }
  },
  "tags": [
    "sensor",
    "adls",
    "monitor"
  ],
  "name": "Adls Monitor"
}