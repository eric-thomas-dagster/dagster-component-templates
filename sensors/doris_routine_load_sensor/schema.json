{
  "component_type": "dagster_component_templates.DorisRoutineLoadSensorComponent",
  "description": "Watch Apache Doris Routine Load jobs for state changes.\n\nExample:\n\n    ```yaml\n    type: dagster_community_components.DorisRoutineLoadSensorComponent\n    attributes:\n      sensor_name: doris_orders_rl_health\n      database: analytics\n      job_name: orders_rl                  # or omit to watch all jobs in db\n      host_env_var: DORIS_FE_HOST\n      query_port: 9030\n      username_env_var: DORIS_USER\n      password_env_var: DORIS_PASSWORD\n      job_name_target: alert_oncall_job\n      asset_key: doris/analytics/events_realtime\n    ```",
  "category": "sensor",
  "icon": "si:apache",
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "required": true,
      "description": "Unique sensor name."
    },
    "database": {
      "type": "string",
      "label": "Database",
      "required": true,
      "description": "Doris database to watch."
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "required": false,
      "description": "Specific Routine Load job name. If unset, watches all jobs in the database and fires an event per state change observed.",
      "default": null
    },
    "job_name_target": {
      "type": "string",
      "label": "Job Name Target",
      "required": true,
      "description": "Dagster job to trigger when an unhealthy state is detected."
    },
    "asset_key": {
      "type": "string",
      "label": "Asset Key",
      "required": false,
      "description": "Optional asset key for the destination table. Sensor emits AssetObservation (default — Routine Load is an external ingestion path; observation is the right shape) on state changes.",
      "default": null
    },
    "host_env_var": {
      "type": "string",
      "label": "Host Env Var",
      "required": false,
      "description": "Env var with Doris FE host.",
      "default": "DORIS_FE_HOST"
    },
    "query_port": {
      "type": "integer",
      "label": "Query Port",
      "required": false,
      "description": "Doris MySQL-protocol query port.",
      "default": 9030
    },
    "username_env_var": {
      "type": "string",
      "label": "Username Env Var",
      "required": false,
      "description": "Env var with username.",
      "default": "DORIS_USER"
    },
    "password_env_var": {
      "type": "string",
      "label": "Password Env Var",
      "required": false,
      "description": "Env var with password.",
      "default": "DORIS_PASSWORD"
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "required": false,
      "description": "Seconds between polls.",
      "default": 60
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "required": false,
      "description": "'running' or 'stopped'.",
      "default": "running",
      "enum": [
        "running",
        "stopped"
      ],
      "ui:widget": "select"
    },
    "emit_materialization": {
      "type": "boolean",
      "label": "Emit Materialization",
      "required": false,
      "description": "When True (default), emit AssetMaterialization on the target asset key. External assets show healthy/green in the Dagster UI and downstream AutomationCondition.eager() fires naturally on parent updates. When False, emit AssetObservation — free of Dagster+ credit charges, but the target asset renders as observed-external (dashed border, gray) and downstream conditions that gate on ~any_deps_missing() (including eager()) will not fire. Both event types carry the same dagster/data_version tag.",
      "default": true
    }
  }
}
