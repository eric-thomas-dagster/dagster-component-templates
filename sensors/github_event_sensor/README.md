# GithubEventSensor

Polls the GitHub Events API for a repo and triggers a RunRequest for each new event of the specified types. Cursor stored as last-seen event ID. Personal access token used via env var.

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `sensor_name` | `str` | Unique sensor name. |
| `asset_keys` | `List[str]` | Slash-separated asset keys to materialize when an event matches. |
| `repo` | `str` | GitHub repo in 'owner/name' form. |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `token_env_var` | `str` | `"GITHUB_TOKEN"` | Env var with a GitHub PAT (or unset for public repos with rate-limited unauthed access). |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `minimum_interval_seconds` | `int` | `60` | Minimum seconds between sensor evaluations. |
| `default_status` | `str` | `"STOPPED"` | 'RUNNING' or 'STOPPED'. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `event_types` | `List[str]` | `['PullRequestEvent', 'PushEvent', 'IssuesEvent']` | GitHub event_type names to listen for. |

[//]: # (FIELDS:END)

## Example YAML
```yaml
type: dagster_component_templates.GithubEventSensorComponent
attributes:
  sensor_name: dagster_pr_sensor
  asset_keys: [open_pr_count]
  repo: "dagster-io/dagster"
  token_env_var: GITHUB_TOKEN
  event_types: [PullRequestEvent]
  minimum_interval_seconds: 300
```

## Requirements

```
requests
```
