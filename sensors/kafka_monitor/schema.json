{
  "component_type": "dagster_component_templates.KafkaMonitorSensorComponent",
  "description": "Component for monitoring a Kafka topic for new messages.",
  "category": "sensor",
  "icon": "Radar",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor",
      "required": true
    },
    "bootstrap_servers": {
      "type": "string",
      "label": "Bootstrap Servers",
      "description": "Comma-separated list of Kafka broker addresses (e.g., ",
      "required": true
    },
    "topic": {
      "type": "string",
      "label": "Topic",
      "description": "Name of the Kafka topic to monitor",
      "required": true
    },
    "group_id": {
      "type": "string",
      "label": "Group Id",
      "description": "Kafka consumer group ID for this sensor",
      "required": true
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "description": "Name of the job to trigger when messages are detected",
      "required": true
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum Interval Seconds",
      "required": false,
      "default": 30
    },
    "max_messages_per_poll": {
      "type": "integer",
      "label": "Max Messages Per Poll",
      "description": "Maximum number of messages to consume per sensor evaluation",
      "required": false,
      "default": 100
    },
    "poll_timeout_seconds": {
      "type": "number",
      "label": "Poll Timeout Seconds",
      "description": "Seconds to wait for messages during each poll call",
      "required": false,
      "default": 5.0
    },
    "security_protocol": {
      "type": "string",
      "label": "Security Protocol",
      "description": "Security Protocol",
      "required": false,
      "default": "PLAINTEXT"
    },
    "sasl_mechanism": {
      "type": "string",
      "label": "Sasl Mechanism",
      "description": "Sasl Mechanism",
      "required": false,
      "default": null
    },
    "sasl_username_env_var": {
      "type": "string",
      "label": "Sasl Username Env Var",
      "description": "Name of the environment variable containing the SASL username",
      "required": false,
      "default": null
    },
    "sasl_password_env_var": {
      "type": "string",
      "label": "Sasl Password Env Var",
      "description": "Name of the environment variable containing the SASL password",
      "required": false,
      "default": null,
      "ui:widget": "password"
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key providing a pre-configured client. ",
      "required": false,
      "default": null
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "description": "Default Status",
      "required": false,
      "default": "running"
    }
  },
  "tags": [
    "sensor",
    "kafka",
    "monitor"
  ]
}