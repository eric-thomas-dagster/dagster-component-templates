{
  "component_type": "dagster_component_templates.MLflowModelSensor",
  "description": "Component that fires a RunRequest when an MLflow model transitions to a",
  "category": "sensor",
  "icon": "si:mlflow",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor.",
      "required": true
    },
    "tracking_uri_env_var": {
      "type": "string",
      "label": "Tracking Uri Env Var",
      "description": "Name of the environment variable that holds the MLflow Tracking URI (e.g., MLFLOW_TRACKING_URI).  The variable must be set at runtime.",
      "required": true
    },
    "model_name": {
      "type": "string",
      "label": "Model Name",
      "description": "Registered model name to watch in the MLflow Model Registry.",
      "required": true
    },
    "target_stage": {
      "type": "string",
      "label": "Target Stage",
      "description": "Model Registry stage transition that triggers a run. Common values: 'Production', 'Staging', 'Archived'.",
      "required": false,
      "default": "Production"
    },
    "target_job": {
      "type": "string",
      "label": "Target Job",
      "description": "Name of the Dagster job to trigger when a new version is detected.",
      "required": true
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum time (in seconds) between sensor evaluations.",
      "required": false,
      "default": 60
    },
    "run_config": {
      "type": "object",
      "label": "Run Config",
      "description": "Optional extra run config merged into the triggered job's run_config. Keys are merged under the top-level 'ops' key alongside the model metadata injected by the sensor.",
      "required": false,
      "default": null,
      "ui:widget": "key-value"
    }
  },
  "tags": [
    "sensor",
    "mlflow",
    "model"
  ],
  "name": "MLflow Model Sensor"
}
