# PagerDutyIncidentSensor

Polls PagerDuty's REST API for incidents. Triggers a RunRequest per new incident since the cursor (incident `created_at`). Token-authed.

[//]: # (FIELDS:START - auto-generated by tools/regen_readme_fields.py)

## Fields

### Required

| Field | Type | Description |
|---|---|---|
| `sensor_name` | `str` | Unique sensor name. |
| `asset_keys` | `List[str]` | Slash-separated asset keys to materialize when an event matches. |

### Connection

| Field | Type | Default | Description |
|---|---|---|---|
| `api_key_env_var` | `str` | `"PAGERDUTY_API_KEY"` | Env var with the PagerDuty REST API key. |

### Sensor configuration

| Field | Type | Default | Description |
|---|---|---|---|
| `minimum_interval_seconds` | `int` | `60` | Minimum seconds between sensor evaluations. |
| `default_status` | `str` | `"STOPPED"` | 'RUNNING' or 'STOPPED'. |

### Other

| Field | Type | Default | Description |
|---|---|---|---|
| `statuses` | `List[str]` | `['triggered', 'acknowledged']` | Incident statuses to listen for. |
| `urgencies` | `List[str]` | `['high']` | Incident urgencies to listen for. |

[//]: # (FIELDS:END)

## Example YAML
```yaml
type: dagster_component_templates.PagerDutyIncidentSensorComponent
attributes:
  sensor_name: high_severity_incident_sensor
  asset_keys: [incident_post_mortem]
  api_key_env_var: PAGERDUTY_API_KEY
  statuses: [triggered]
  urgencies: [high]
  minimum_interval_seconds: 60
```

## Requirements

```
requests
```
