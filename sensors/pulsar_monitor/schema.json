{
  "component_type": "dagster_component_templates.PulsarMonitorSensorComponent",
  "description": "Component for monitoring an Apache Pulsar topic for new messages.",
  "category": "sensor",
  "icon": "Radar",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor",
      "required": true
    },
    "service_url": {
      "type": "string",
      "label": "Service Url",
      "description": "Pulsar service URL (e.g., ",
      "required": true
    },
    "topic": {
      "type": "string",
      "label": "Topic",
      "description": "Pulsar topic name (e.g., ",
      "required": true
    },
    "subscription_name": {
      "type": "string",
      "label": "Subscription Name",
      "description": "Durable subscription name. Pulsar tracks the position per subscription.",
      "required": true,
      "ui:widget": "code"
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "description": "Name of the job to trigger when messages are detected",
      "required": true
    },
    "subscription_type": {
      "type": "string",
      "label": "Subscription Type",
      "description": "Subscription type: Exclusive, Shared, Failover, or Key_Shared",
      "required": false,
      "default": "Shared",
      "ui:widget": "code"
    },
    "max_messages_per_poll": {
      "type": "integer",
      "label": "Max Messages Per Poll",
      "description": "Maximum number of messages to consume per evaluation",
      "required": false,
      "default": 100
    },
    "receive_timeout_ms": {
      "type": "integer",
      "label": "Receive Timeout Ms",
      "description": "Milliseconds to wait for each message before stopping the poll",
      "required": false,
      "default": 5000
    },
    "jwt_token_env_var": {
      "type": "string",
      "label": "Jwt Token Env Var",
      "description": "Name of the environment variable containing a JWT auth token",
      "required": false,
      "default": null
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum Interval Seconds",
      "required": false,
      "default": 30
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key providing a pre-configured client. ",
      "required": false,
      "default": null,
      "ui:widget": "resource-picker"
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "description": "Default Status",
      "required": false,
      "default": "running"
    }
  },
  "tags": [
    "sensor",
    "pulsar",
    "monitor"
  ]
}