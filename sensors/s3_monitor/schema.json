{
  "component_type": "dagster_component_templates.S3MonitorSensorComponent",
  "description": "Component for monitoring an S3 bucket prefix for new objects.",
  "category": "sensor",
  "icon": "Radar",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor",
      "required": true
    },
    "bucket_name": {
      "type": "string",
      "label": "Bucket Name",
      "description": "Name of the S3 bucket to monitor",
      "required": true
    },
    "prefix": {
      "type": "string",
      "label": "Prefix",
      "description": "S3 key prefix to scope monitoring (e.g., ",
      "required": false,
      "default": ""
    },
    "key_pattern": {
      "type": "string",
      "label": "Key Pattern",
      "description": "Regex pattern to match object keys (e.g., ",
      "required": false,
      "default": ".*"
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "description": "Name of the job to trigger when objects are detected",
      "required": true
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum Interval Seconds",
      "required": false,
      "default": 30
    },
    "region_name": {
      "type": "string",
      "label": "Region Name",
      "description": "AWS region name (e.g., ",
      "required": false,
      "default": null
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key providing an AWS client. When set, ",
      "required": false,
      "default": null,
      "ui:widget": "resource-picker"
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "description": "Default Status",
      "required": false,
      "default": "running"
    }
  },
  "tags": [
    "sensor",
    "monitor"
  ],
  "name": "S3 Monitor"
}