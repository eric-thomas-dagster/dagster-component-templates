{
  "component_type": "dagster_component_templates.ServiceBusMonitorSensorComponent",
  "description": "Component for polling an Azure Service Bus queue or topic subscription.",
  "category": "sensor",
  "icon": "Radar",
  "x-dagster-io": {
    "outputs": {
      "type": "RunRequest",
      "description": "Sensor run requests"
    }
  },
  "attributes": {
    "sensor_name": {
      "type": "string",
      "label": "Sensor Name",
      "description": "Unique name for this sensor",
      "required": true
    },
    "job_name": {
      "type": "string",
      "label": "Job Name",
      "description": "Name of the job to trigger when messages are received",
      "required": true
    },
    "fully_qualified_namespace": {
      "type": "string",
      "label": "Fully Qualified Namespace",
      "description": "Service Bus namespace hostname (e.g., ",
      "required": false,
      "default": null
    },
    "connection_string_env_var": {
      "type": "string",
      "label": "Connection String Env Var",
      "description": "Name of the environment variable containing the Service Bus connection string. ",
      "required": false,
      "default": null
    },
    "queue_name": {
      "type": "string",
      "label": "Queue Name",
      "description": "Name of the Service Bus queue to monitor. Provide this OR topic_name.",
      "required": false,
      "default": null
    },
    "topic_name": {
      "type": "string",
      "label": "Topic Name",
      "description": "Name of the Service Bus topic to monitor. Requires subscription_name.",
      "required": false,
      "default": null
    },
    "subscription_name": {
      "type": "string",
      "label": "Subscription Name",
      "description": "Subscription Name",
      "required": false,
      "default": null,
      "ui:widget": "code"
    },
    "max_messages_per_poll": {
      "type": "integer",
      "label": "Max Messages Per Poll",
      "description": "Maximum number of messages to receive per evaluation",
      "required": false,
      "default": 100
    },
    "minimum_interval_seconds": {
      "type": "integer",
      "label": "Minimum Interval Seconds",
      "description": "Minimum Interval Seconds",
      "required": false,
      "default": 30
    },
    "resource_key": {
      "type": "string",
      "label": "Resource Key",
      "description": "Optional Dagster resource key providing a pre-configured client. ",
      "required": false,
      "default": null,
      "ui:widget": "resource-picker"
    },
    "default_status": {
      "type": "string",
      "label": "Default Status",
      "description": "Default Status",
      "required": false,
      "default": "running"
    }
  },
  "tags": [
    "sensor",
    "servicebus",
    "monitor"
  ]
}